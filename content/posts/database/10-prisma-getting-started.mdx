---
title: "10. Prisma ORM 入门：现代化类型安全的数据库访问"
date: "2026-03-19"
summary: "学习 Prisma ORM 的核心概念，掌握类型安全的数据库访问方式"
tags: ["Prisma", "ORM", "TypeScript", "PostgreSQL"]
category: "数据库"
cover: ""
draft: false
series: "数据库渐进式学习路线"
seriesOrder: 10
---

## 前言

写过原生 SQL 吗？繁琐、易错、没有类型提示... Prisma ORM 让数据库访问变得简单、类型安全、开发体验极佳！

Prisma 是下一代 ORM，提供完整的类型安全、优秀的开发体验和强大的查询能力。本文将从零开始，带你掌握 Prisma 的核心用法。

---

## 核心概念

### 为什么选择 Prisma？

**Prisma 优势**：

- ✅ 完整的 TypeScript 类型安全
- ✅ 自动生成类型定义
- ✅ 强大的查询 API
- ✅ 可视化数据库管理（Prisma Studio）
- ✅ 优秀的迁移系统

**Prisma vs 其他 ORM**：

| 特性     | Prisma     | TypeORM  | Sequelize |
| -------- | ---------- | -------- | --------- |
| 类型安全 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐    |
| API 设计 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐   | ⭐⭐⭐    |
| 迁移工具 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐   | ⭐⭐      |
| 学习曲线 | 简单       | 中等     | 中等      |
| 文档质量 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐    |

---

## Prisma 核心组件

**Prisma 由三部分组成**：

1. **Prisma Schema**：数据模型定义（schema.prisma）
2. **Prisma Client**：类型安全的查询客户端
3. **Prisma Migrate**：数据库迁移工具
4. **Prisma Studio**：可视化管理界面

---

## 快速开始

### 1. 初始化项目

```bash
# 创建项目
mkdir prisma-demo
cd prisma-demo
npm init -y

# 安装 Prisma
npm install prisma @prisma/client

# 初始化 Prisma
npx prisma init

# 目录结构
.
├── prisma
│   └── schema.prisma    # 数据模型定义
├── node_modules
└── package.json
```

---

### 2. 配置数据库连接

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

```env
# .env
DATABASE_URL="postgresql://dev:dev123@localhost:5432/dev_db?schema=public"
```

---

### 3. 定义数据模型

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum OrderStatus {
  PENDING
  PAID
  SHIPPED
  DELIVERED
  CANCELLED
}

model User {
  id            String         @id @default(uuid())
  email         String         @unique
  passwordHash  String         @map("password_hash")
  fullName      String?        @map("full_name")
  phone         String?
  avatarUrl     String?        @map("avatar_url")
  profile       Json?
  preferences   Json           @default("{}")
  isVerified    Boolean        @default(false) @map("is_verified")
  lastLoginAt   DateTime?      @map("last_login_at")
  createdAt     DateTime       @default(now()) @map("created_at")
  updatedAt     DateTime       @updatedAt @map("updated_at")

  orders        Order[]
  profile       UserProfile?

  @@index([email])
  @@index([isVerified])
  @@map("users")
}

model UserProfile {
  userId      String   @id @map("user_id")
  bio         String?
  website     String?
  location    String?
  birthDate   DateTime? @map("birth_date")
  gender      String?

  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("user_profiles")
}

model Order {
  id            String      @id @default(uuid())
  userId        String      @map("user_id")
  orderNumber   String      @unique @map("order_number")
  status        OrderStatus @default(PENDING)
  subtotal      Decimal     @db.Decimal(10, 2)
  tax           Decimal     @db.Decimal(10, 2) @default(0)
  discount      Decimal     @db.Decimal(10, 2) @default(0)
  total         Decimal     @db.Decimal(10, 2)
  notes         String?
  metadata      Json?
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")

  user          User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  items         OrderItem[]

  @@index([userId])
  @@index([status])
  @@index([createdAt])
  @@map("orders")
}

model Product {
  id          String    @id @default(uuid())
  name        String
  slug        String    @unique
  description String?
  price       Decimal   @db.Decimal(10, 2)
  stock       Int       @default(0)
  categoryId  String?   @map("category_id")
  createdAt   DateTime  @default(now()) @map("created_at")

  items       OrderItem[]

  @@index([slug])
  @@index([categoryId])
  @@map("products")
}

model OrderItem {
  id         String   @id @default(uuid())
  orderId    String   @map("order_id")
  productId  String   @map("product_id")
  quantity   Int
  unitPrice  Decimal  @map("unit_price") @db.Decimal(10, 2)
  subtotal   Decimal  @db.Decimal(10, 2)
  createdAt  DateTime @default(now()) @map("created_at")

  order      Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  product    Product  @relation(fields: [productId], references: [id], onDelete: Restrict)

  @@index([orderId])
  @@index([productId])
  @@map("order_items")
}
```

---

### 4. 创建迁移

```bash
# 创建迁移
npx prisma migrate dev --name init

# 输出
# ✅ The migration was created
# ✔ Generated Prisma Client
```

---

### 5. 生成 Prisma Client

```bash
# 生成 Prisma Client
npx prisma generate
```

---

## CRUD 操作

### 1. 初始化 Prisma Client

```typescript
// src/lib/prisma.ts
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: ["query", "error", "warn"],
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;
```

---

### 2. Create（创建）

```typescript
// 创建用户
const user = await prisma.user.create({
  data: {
    email: "user@example.com",
    passwordHash: "hashed_password",
    fullName: "张三",
  },
});

// 创建关联数据
const order = await prisma.order.create({
  data: {
    userId: "user-123",
    orderNumber: "ORD-001",
    total: 100,
    items: {
      create: [
        {
          productId: "prod-1",
          quantity: 2,
          unitPrice: 50,
          subtotal: 100,
        },
      ],
    },
  },
});
```

---

### 3. Read（读取）

```typescript
// 查询单个用户
const user = await prisma.user.findUnique({
  where: { id: "user-123" },
});

// 查询用户（包含关联）
const userWithOrders = await prisma.user.findUnique({
  where: { id: "user-123" },
  include: {
    orders: {
      where: { status: "PAID" },
      orderBy: { createdAt: "desc" },
      take: 10,
    },
    profile: true,
  },
});

// 查询多个用户
const users = await prisma.user.findMany({
  where: {
    isVerified: true,
    email: { endsWith: "@example.com" },
  },
  select: {
    id: true,
    email: true,
    fullName: true,
  },
  orderBy: { createdAt: "desc" },
  take: 20,
});
```

---

### 4. Update（更新）

```typescript
// 更新用户
const updatedUser = await prisma.user.update({
  where: { id: "user-123" },
  data: {
    lastLoginAt: new Date(),
    isVerified: true,
  },
});

// 更新或创建（upsert）
const user = await prisma.user.upsert({
  where: { email: "user@example.com" },
  create: {
    email: "user@example.com",
    passwordHash: "hash",
  },
  update: {
    lastLoginAt: new Date(),
  },
});
```

---

### 5. Delete（删除）

```typescript
// 删除用户
await prisma.user.delete({
  where: { id: "user-123" },
});

// 批量删除
await prisma.user.deleteMany({
  where: {
    isVerified: false,
    createdAt: { lt: new Date("2024-01-01") },
  },
});
```

---

## 事务操作

```typescript
// 方式 1：$transaction API
await prisma.$transaction(async (tx) => {
  // 创建订单
  const order = await tx.order.create({
    data: {
      userId,
      orderNumber: `ORD-${Date.now()}`,
      total: 100,
    },
  });

  // 创建订单项
  await tx.orderItem.create({
    data: {
      orderId: order.id,
      productId,
      quantity: 2,
      unitPrice: 50,
      subtotal: 100,
    },
  });

  // 更新库存
  await tx.product.update({
    where: { id: productId },
    data: { stock: { decrement: 2 } },
  });

  return order;
});

// 方式 2：批量操作（独立事务）
const [user, order] = await prisma.$transaction([
  prisma.user.create({ data: userData }),
  prisma.order.create({ data: orderData }),
]);
```

---

## 最佳实践

### 1. 类型安全

```typescript
// ✅ 完全类型安全
const user: User = await prisma.user.findUnique({
  where: { id: "user-123" },
});

// ✅ 自动补全
const users = await prisma.user.findMany({
  where: {
    email: { endsWith: "@example.com" }, // IDE 自动提示可用方法
  },
});
```

---

### 2. 连接池配置

```typescript
// prisma/schema.prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  // 连接池配置
  pool_timeout = 30
  connection_limit = 10
}
```

---

### 3. 环境变量

```env
# .env
DATABASE_URL="postgresql://dev:dev123@localhost:5432/dev_db?schema=public&connection_limit=10"

# 生产环境
# DATABASE_URL="postgresql://user:pass@prod-host:5432/prod_db?schema=public&connection_limit=50&pool_timeout=20"
```

---

## 常见问题

### Q1: 如何查看生成的 SQL？

```typescript
const prisma = new PrismaClient({
  log: ["query", "info", "warn", "error"],
});
```

---

### Q2: 如何处理大数据量？

```typescript
// 使用 cursor 分页
const firstPage = await prisma.order.findMany({
  take: 20,
});

const lastItem = firstPage[19];
const nextPage = await prisma.order.findMany({
  take: 20,
  skip: 1,
  cursor: { id: lastItem.id },
});
```

---

## 总结

本文系统学习了 Prisma ORM：

**核心概念**：

- Prisma Schema、Prisma Client、Prisma Migrate
- 完整的 TypeScript 类型安全
- 自动生成类型定义

**CRUD 操作**：

- Create、Read、Update、Delete
- 关联查询
- 事务操作

**最佳实践**：

- 类型安全
- 连接池配置
- 环境变量管理

---

## 下一步

掌握了 Prisma 入门后，下一步我们将学习：

**《11. Prisma 高级查询》**：掌握 Prisma 的高级查询技巧和性能优化。

你将学习：

- 🔍 复杂过滤与排序
- 📊 聚合查询
- 🔗 关系查询优化
- ⚡ 性能优化技巧

继续关注，让我们一起深入学习 Prisma！
