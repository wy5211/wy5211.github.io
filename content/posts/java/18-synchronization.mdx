---
title: "18 同步与并发工具"
date: "2026-03-21"
summary: "掌握 Java 线程同步机制，学会使用并发工具类"
tags: ["Java", "同步", "锁", "并发工具", "线程安全"]
category: "java"
draft: false
series: "Java 渐进式语法学习"
seriesOrder: 18
---

# 同步与并发工具

## 前言

多线程环境下，共享数据的访问需要同步机制来保证线程安全。Java 提供了多种同步机制和并发工具类。本文将介绍 synchronized、Lock 以及常用的并发工具。

## synchronized 关键字

### 同步方法

```java
public class SynchronizedMethod {

    private int count = 0;

    // 同步实例方法
    public synchronized void increment() {
        count++;
    }

    // 同步静态方法
    public static synchronized void staticMethod() {
        // ...
    }

    public static void main(String[] args) throws InterruptedException {
        SynchronizedMethod obj = new SynchronizedMethod();

        Thread thread1 = new Thread(() -> {
            for (int i = 0; i < 10000; i++) {
                obj.increment();
            }
        });

        Thread thread2 = new Thread(() -> {
            for (int i = 0; i < 10000; i++) {
                obj.increment();
            }
        });

        thread1.start();
        thread2.start();

        thread1.join();
        thread2.join();

        System.out.println("Count：" + obj.count);  // 20000
    }
}
```

### 同步代码块

```java
public class SynchronizedBlock {

    private int count = 0;
    private Object lock = new Object();

    public void increment() {
        synchronized (lock) {  // 使用对象锁
            count++;
        }
    }

    public void decrement() {
        synchronized (lock) {
            count--;
        }
    }
}
```

## Lock 接口

### ReentrantLock

```java
import java.util.concurrent.locks.*;

public class LockExample {

    private int count = 0;
    private final Lock lock = new ReentrantLock();

    public void increment() {
        lock.lock();  // 获取锁
        try {
            count++;
        } finally {
            lock.unlock();  // 释放锁
        }
    }

    public static void main(String[] args) throws InterruptedException {
        LockExample example = new LockExample();

        Thread thread1 = new Thread(() -> {
            for (int i = 0; i < 10000; i++) {
                example.increment();
            }
        });

        Thread thread2 = new Thread(() -> {
            for (int i = 0; i < 10000; i++) {
                example.increment();
            }
        });

        thread1.start();
        thread2.start();

        thread1.join();
        thread2.join();

        System.out.println("Count：" + example.count);  // 20000
    }
}
```

### tryLock

```java
import java.util.concurrent.locks.*;

public class TryLockExample {

    private final Lock lock = new ReentrantLock();

    public void doWork() {
        if (lock.tryLock()) {  // 尝试获取锁
            try {
                // 执行任务
            } finally {
                lock.unlock();
            }
        } else {
            // 获取锁失败，执行其他操作
            System.out.println("无法获取锁");
        }
    }
}
```

## 读写锁

```java
import java.util.concurrent.locks.*;

public class ReadWriteLockExample {

    private final ReadWriteLock rwLock = new ReentrantReadWriteLock();
    private final Lock readLock = rwLock.readLock();
    private final Lock writeLock = rwLock.writeLock();

    private volatile String data = "初始数据";

    // 读操作（多个线程可以同时读取）
    public String readData() {
        readLock.lock();
        try {
            System.out.println(Thread.currentThread().getName() + " 读取数据：" + data);
            return data;
        } finally {
            readLock.unlock();
        }
    }

    // 写操作（只能有一个线程写入）
    public void writeData(String newData) {
        writeLock.lock();
        try {
            System.out.println(Thread.currentThread().getName() + " 写入数据：" + newData);
            data = newData;
        } finally {
            writeLock.unlock();
        }
    }

    public static void main(String[] args) {
        ReadWriteLockExample example = new ReadWriteLockExample();

        // 多个读线程
        for (int i = 0; i < 3; i++) {
            new Thread(() -> {
                example.readData();
            }, "读线程" + i).start();
        }

        // 一个写线程
        new Thread(() -> {
            example.writeData("新数据");
        }, "写线程").start();
    }
}
```

## volatile 关键字

```java
public class VolatileExample {

    private volatile boolean running = true;

    public void stop() {
        running = false;
    }

    public void doWork() {
        while (running) {
            // 执行任务
        }
        System.out.println("线程停止");
    }

    public static void main(String[] args) throws InterruptedException {
        VolatileExample example = new VolatileExample();

        Thread worker = new Thread(() -> example.doWork());
        worker.start();

        Thread.sleep(1000);
        example.stop();
        worker.join();
    }
}
```

## 并发工具类

### CountDownLatch

```java
import java.util.concurrent.*;

public class CountDownLatchDemo {

    public static void main(String[] args) throws InterruptedException {
        CountDownLatch latch = new CountDownLatch(3);

        for (int i = 1; i <= 3; i++) {
            final int taskId = i;
            new Thread(() -> {
                try {
                    System.out.println("任务 " + taskId + " 开始");
                    Thread.sleep(1000);
                    System.out.println("任务 " + taskId + " 完成");
                } catch (InterruptedException e) {
                    e.printStackTrace();
                } finally {
                    latch.countDown();  // 计数减 1
                }
            }).start();
        }

        latch.await();  // 等待所有任务完成
        System.out.println("所有任务完成，主线程继续");
    }
}
```

### CyclicBarrier

```java
import java.util.concurrent.*;

public class CyclicBarrierDemo {

    public static void main(String[] args) {
        int parties = 3;
        CyclicBarrier barrier = new CyclicBarrier(parties, () -> {
            System.out.println("所有线程已到达栅栏，继续执行");
        });

        for (int i = 1; i <= parties; i++) {
            final int threadId = i;
            new Thread(() -> {
                try {
                    System.out.println("线程 " + threadId + " 到达");
                    barrier.await();  // 等待其他线程
                    System.out.println("线程 " + threadId + " 继续");
                } catch (Exception e) {
                    e.printStackTrace();
                }
            }).start();
        }
    }
}
```

### Semaphore

```java
import java.util.concurrent.*;

public class SemaphoreDemo {

    public static void main(String[] args) {
        Semaphore semaphore = new Semaphore(2);  // 只允许 2 个线程同时访问

        for (int i = 1; i <= 5; i++) {
            final int threadId = i;
            new Thread(() -> {
                try {
                    semaphore.acquire();  // 获取许可
                    System.out.println("线程 " + threadId + " 获得许可");
                    Thread.sleep(1000);
                    System.out.println("线程 " + threadId + " 释放许可");
                } catch (InterruptedException e) {
                    e.printStackTrace();
                } finally {
                    semaphore.release();  // 释放许可
                }
            }).start();
        }
    }
}
```

## 实战演练

### 示例 1：银行转账

```java
import java.util.concurrent.locks.*;

public class BankTransfer {

    static class Account {
        private String name;
        private double balance;
        private final Lock lock = new ReentrantLock();

        public Account(String name, double balance) {
            this.name = name;
            this.balance = balance;
        }

        public void debit(double amount) {
            balance -= amount;
        }

        public void credit(double amount) {
            balance += amount;
        }

        public Lock getLock() {
            return lock;
        }

        @Override
        public String toString() {
            return name + "：" + balance;
        }
    }

    public static void transfer(Account from, Account to, double amount) {
        // 获取两个锁，避免死锁
        while (true) {
            if (from.getLock().tryLock()) {
                try {
                    if (to.getLock().tryLock()) {
                        try {
                            if (from.balance >= amount) {
                                from.debit(amount);
                                to.credit(amount);
                                System.out.printf("从 %s 转账 %.2f 到 %s%n",
                                    from.name, amount, to.name);
                            }
                            return;
                        } finally {
                            to.getLock().unlock();
                        }
                    }
                } finally {
                    from.getLock().unlock();
                }
            }
            // 随机等待后重试
            try {
                Thread.sleep(10);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
    }

    public static void main(String[] args) {
        Account account1 = new Account("张三", 1000);
        Account account2 = new Account("李四", 1000);

        // 多次转账
        for (int i = 0; i < 10; i++) {
            new Thread(() -> {
                transfer(account1, account2, 100);
                transfer(account2, account1, 100);
            }).start();
        }
    }
}
```

### 示例 2：缓存系统

```java
import java.util.concurrent.locks.*;
import java.util.*;

public class CacheSystem<K, V> {

    private final Map<K, V> cache = new HashMap<>();
    private final ReadWriteLock rwLock = new ReentrantReadWriteLock();
    private final Lock readLock = rwLock.readLock();
    private final Lock writeLock = rwLock.writeLock();

    public V get(K key) {
        readLock.lock();
        try {
            return cache.get(key);
        } finally {
            readLock.unlock();
        }
    }

    public void put(K key, V value) {
        writeLock.lock();
        try {
            cache.put(key, value);
        } finally {
            writeLock.unlock();
        }
    }

    public void clear() {
        writeLock.lock();
        try {
            cache.clear();
        } finally {
            writeLock.unlock();
        }
    }

    public static void main(String[] args) {
        CacheSystem<String, String> cache = new CacheSystem<>();

        // 多线程读写
        for (int i = 0; i < 10; i++) {
            final int index = i;
            new Thread(() -> {
                cache.put("key" + index, "value" + index);
                System.out.println(cache.get("key" + index));
            }).start();
        }
    }
}
```

## 最佳实践

### 1. 同步块的范围

```java
// ✅ 好的做法：最小化同步范围
synchronized (lock) {
    // 只同步必要的代码
}

// ❌ 不好的做法：同步范围过大
synchronized (lock) {
    // 包含不必要的代码
    Thread.sleep(1000);  // 长时间操作
}
```

### 2. 使用 ReentrantLock

```java
// ✅ 好的做法：在 finally 中释放锁
lock.lock();
try {
    // 临界区代码
} finally {
    lock.unlock();
}
```

### 3. 避免死锁

```java
// ✅ 好的做法：按固定顺序获取锁
synchronized (lock1) {
    synchronized (lock2) {
        // 操作
    }
}

// ❌ 不好的做法：不同顺序获取锁
// 线程1：lock1 → lock2
// 线程2：lock2 → lock1（可能死锁）
```

## 常见问题

### Q1：synchronized 和 Lock 的区别？

**答**：Lock 更灵活，支持 tryLock，可以中断获取锁。

### Q2：volatile 的作用？

**答**：保证可见性和有序性，不保证原子性。

### Q3：何时使用读写锁？

**答**：读多写少的场景，提高并发性能。

## 总结

本文学习了：

✅ **synchronized**

- 同步方法
- 同步代码块

✅ **Lock 接口**

- ReentrantLock
- tryLock

✅ **读写锁**

- ReadWriteLock
- ReentrantReadWriteLock

✅ **volatile**

- 保证可见性

✅ **并发工具**

- CountDownLatch
- CyclicBarrier
- Semaphore

## 下一步

下一篇文章将学习 Lambda 表达式。

---

- **上一篇**：[17 多线程基础](./17-multi-threading-basics.mdx)
- **下一篇**：[19 Lambda 表达式](./19-lambda-expressions.mdx)
