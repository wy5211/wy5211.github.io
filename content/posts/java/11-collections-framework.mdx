---
title: "11 集合框架概览"
date: "2026-03-21"
summary: "全面了解 Java 集合框架体系，掌握核心接口和实现类"
tags: ["Java", "集合框架", "List", "Set", "Map"]
category: "java"
draft: false
series: "Java 渐进式语法学习"
seriesOrder: 11
---

# 集合框架概览

## 前言

集合是存储和操作对象的容器。Java 集合框架提供了一套性能优良、使用方便的接口和类。掌握集合框架是 Java 开发者的必备技能。

## 集合框架体系

```
Collection (接口)
├── List (接口)
│   ├── ArrayList (类)
│   ├── LinkedList (类)
│   └── Vector (类)
├── Set (接口)
│   ├── HashSet (类)
│   ├── LinkedHashSet (类)
│   └── TreeSet (类)
└── Queue (接口)
    ├── LinkedList (类)
    └── PriorityQueue (类)

Map (接口)
├── HashMap (类)
├── LinkedHashMap (类)
├── TreeMap (类)
└── Hashtable (类)
```

## Collection 接口

### Collection 常用方法

```java
import java.util.*;

public class CollectionDemo {

    public static void main(String[] args) {
        Collection<String> collection = new ArrayList<>();

        // 添加元素
        collection.add("A");
        collection.add("B");
        collection.add("C");

        // 判断是否包含元素
        System.out.println("包含 B？" + collection.contains("B"));

        // 删除元素
        collection.remove("A");

        // 集合大小
        System.out.println("大小：" + collection.size());

        // 判断是否为空
        System.out.println("为空？" + collection.isEmpty());

        // 转为数组
        Object[] array = collection.toArray();

        // 清空集合
        collection.clear();
    }
}
```

## List 接口

### ArrayList

```java
import java.util.*;

public class ArrayListDemo {

    public static void main(String[] args) {
        // 创建 ArrayList
        List<String> list = new ArrayList<>();

        // 添加元素
        list.add("Java");
        list.add("Python");
        list.add("C++");

        // 访问元素
        System.out.println("第一个元素：" + list.get(0));

        // 修改元素
        list.set(0, "JavaScript");
        System.out.println("修改后：" + list.get(0));

        // 删除元素
        list.remove(1);

        // 遍历
        for (String language : list) {
            System.out.println(language);
        }

        // 获取索引
        int index = list.indexOf("C++");
        System.out.println("C++ 的位置：" + index);
    }
}
```

### LinkedList

```java
import java.util.*;

public class LinkedListDemo {

    public static void main(String[] args) {
        LinkedList<String> list = new LinkedList<>();

        // 添加元素
        list.add("A");
        list.addFirst("B");  // 在头部添加
        list.addLast("C");   // 在尾部添加

        // 获取元素
        System.out.println("第一个：" + list.getFirst());
        System.out.println("最后一个：" + list.getLast());

        // 删除元素
        list.removeFirst();
        list.removeLast();

        // 实现栈
        list.push("D");      // 入栈
        String top = list.pop();  // 出栈

        // 实现队列
        list.offer("E");    // 入队
        String front = list.poll();  // 出队
    }
}
```

## Set 接口

### HashSet

```java
import java.util.*;

public class HashSetDemo {

    public static void main(String[] args) {
        Set<String> set = new HashSet<>();

        // 添加元素（自动去重）
        set.add("A");
        set.add("B");
        set.add("A");  // 重复，不会被添加

        System.out.println("大小：" + set.size());  // 2

        // 遍历（无序）
        for (String item : set) {
            System.out.println(item);
        }

        // 判断包含
        System.out.println("包含 A？" + set.contains("A"));
    }
}
```

### TreeSet

```java
import java.util.*;

public class TreeSetDemo {

    public static void main(String[] args) {
        Set<Integer> set = new TreeSet<>();

        // 添加元素
        set.add(5);
        set.add(2);
        set.add(8);
        set.add(1);

        // 自动排序
        for (Integer num : set) {
            System.out.println(num);  // 1, 2, 5, 8
        }

        // 获取第一个和最后一个
        System.out.println("最小值：" + ((TreeSet) set).first());
        System.out.println("最大值：" + ((TreeSet) set).last());
    }
}
```

## Map 接口

### HashMap

```java
import java.util.*;

public class HashMapDemo {

    public static void main(String[] args) {
        Map<String, Integer> map = new HashMap<>();

        // 添加键值对
        map.put("张三", 25);
        map.put("李四", 30);
        map.put("王五", 28);

        // 获取值
        int age = map.get("张三");
        System.out.println("张三的年龄：" + age);

        // 判断包含
        System.out.println("包含李四？" + map.containsKey("李四"));

        // 删除
        map.remove("王五");

        // 遍历
        for (Map.Entry<String, Integer> entry : map.entrySet()) {
            System.out.println(entry.getKey() + "：" + entry.getValue());
        }

        // 只遍历键
        for (String key : map.keySet()) {
            System.out.println("键：" + key);
        }

        // 只遍历值
        for (Integer value : map.values()) {
            System.out.println("值：" + value);
        }
    }
}
```

### TreeMap

```java
import java.util.*;

public class TreeMapDemo {

    public static void main(String[] args) {
        Map<String, Integer> map = new TreeMap<>();

        map.put("C", 3);
        map.put("A", 1);
        map.put("B", 2);

        // 按键排序
        for (Map.Entry<String, Integer> entry : map.entrySet()) {
            System.out.println(entry.getKey() + "：" + entry.getValue());
        }
        // 输出：
        // A：1
        // B：2
        // C：3
    }
}
```

## Iterator 迭代器

```java
import java.util.*;

public class IteratorDemo {

    public static void main(String[] args) {
        List<String> list = new ArrayList<>();
        list.add("A");
        list.add("B");
        list.add("C");

        // 使用 Iterator 遍历
        Iterator<String> iterator = list.iterator();
        while (iterator.hasNext()) {
            String item = iterator.next();
            System.out.println(item);

            // 安全删除
            if (item.equals("B")) {
                iterator.remove();
            }
        }

        // ListIterator（双向遍历）
        ListIterator<String> listIterator = list.listIterator();
        while (listIterator.hasNext()) {
            System.out.println("下一个：" + listIterator.next());
        }
        while (listIterator.hasPrevious()) {
            System.out.println("上一个：" + listIterator.previous());
        }
    }
}
```

## Collections 工具类

```java
import java.util.*;

public class CollectionsDemo {

    public static void main(String[] args) {
        List<Integer> list = new ArrayList<>();
        list.add(5);
        list.add(2);
        list.add(8);
        list.add(1);

        // 排序
        Collections.sort(list);
        System.out.println("排序后：" + list);

        // 反转
        Collections.reverse(list);
        System.out.println("反转后：" + list);

        // 打乱
        Collections.shuffle(list);
        System.out.println("打乱后：" + list);

        // 查找最大值和最小值
        System.out.println("最大值：" + Collections.max(list));
        System.out.println("最小值：" + Collections.min(list));

        // 创建不可变集合
        List<Integer> unmodifiable = Collections.unmodifiableList(list);
        // unmodifiable.add(10);  // 抛出异常

        // 创建同步集合
        List<Integer> synchronizedList = Collections.synchronizedList(new ArrayList<>());
    }
}
```

## 实战演练

### 示例 1：单词统计

```java
import java.util.*;

public class WordCounter {

    public static void main(String[] args) {
        String text = "Java is great Java is fun";

        // 统计词频
        Map<String, Integer> wordCount = new HashMap<>();

        String[] words = text.split(" ");
        for (String word : words) {
            wordCount.put(word, wordCount.getOrDefault(word, 0) + 1);
        }

        // 输出结果
        for (Map.Entry<String, Integer> entry : wordCount.entrySet()) {
            System.out.println(entry.getKey() + "：" + entry.getValue());
        }
    }
}
```

### 示例 2：学生成绩管理

```java
import java.util.*;

public class StudentManager {

    public static void main(String[] args) {
        // 存储学生信息
        Map<String, List<Integer>> studentGrades = new HashMap<>();

        // 添加学生成绩
        studentGrades.put("张三", Arrays.asList(85, 90, 78));
        studentGrades.put("李四", Arrays.asList(92, 88, 95));

        // 计算平均分
        for (Map.Entry<String, List<Integer>> entry : studentGrades.entrySet()) {
            String name = entry.getKey();
            List<Integer> grades = entry.getValue();

            double average = grades.stream()
                .mapToInt(Integer::intValue)
                .average()
                .orElse(0.0);

            System.out.printf("%s 的平均分：%.2f%n", name, average);
        }
    }
}
```

## 最佳实践

### 1. 选择合适的集合

```java
// 频繁访问 → ArrayList
// 频繁插入删除 → LinkedList
// 需要排序 → TreeSet
// 需要快速查找 → HashSet
// 键值对 → HashMap
```

### 2. 使用接口引用

```java
// ✅ 好的做法
List<String> list = new ArrayList<>();

// ❌ 不好的做法
ArrayList<String> list = new ArrayList<>();
```

### 3. 初始化容量

```java
// ✅ 好的做法
List<String> list = new ArrayList<>(1000);

// ❌ 可能导致扩容
List<String> list = new ArrayList<>();
```

## 常见问题

### Q1：ArrayList 和 LinkedList 的区别？

**答**：

- ArrayList：基于数组，查询快，增删慢
- LinkedList：基于链表，增删快，查询慢

### Q2：HashMap 的工作原理？

**答**：基于哈希表，使用数组和链表/红黑树存储数据。

### Q3：HashSet 如何保证元素唯一？

**答**：通过 hashCode() 和 equals() 方法判断。

## 总结

本文学习了：

✅ **集合框架体系**

- Collection 接口
- Map 接口
- 各实现类特点

✅ **List 接口**

- ArrayList
- LinkedList

✅ **Set 接口**

- HashSet
- TreeSet

✅ **Map 接口**

- HashMap
- TreeMap

✅ **工具类**

- Collections
- Iterator

## 下一步

下一篇文章将深入学习 List 接口。

---

- **上一篇**：[10 异常处理机制](./10-exception-handling.mdx)
- **下一篇**：[12 List 接口与实现](./12-list-implementation.mdx)
