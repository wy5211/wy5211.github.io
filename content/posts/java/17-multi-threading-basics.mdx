---
title: "17 多线程基础"
date: "2026-03-21"
summary: "掌握 Java 多线程基础，理解线程的创建和使用"
tags: ["Java", "多线程", "Thread", "Runnable", "并发"]
category: "java"
draft: false
series: "Java 渐进式语法学习"
seriesOrder: 17
---

# 多线程基础

## 前言

多线程让程序能够同时执行多个任务，充分利用 CPU 资源。Java 提供了丰富的多线程支持，本文将介绍线程的创建、生命周期和基本使用。

## 线程概述

### 进程与线程

- **进程**：资源分配的基本单位
- **线程**：CPU 调度的基本单位

```
进程（一个程序）
├── 线程1（主线程）
├── 线程2（工作线程）
└── 线程3（工作线程）
```

### 为什么使用多线程

1. 提高程序响应速度
2. 充分利用 CPU 资源
3. 简化程序模型

## 创建线程

### 方式一：继承 Thread

```java
public class MyThread extends Thread {

    @Override
    public void run() {
        for (int i = 0; i < 5; i++) {
            System.out.println(getName() + "：" + i);
            try {
                Thread.sleep(1000);  // 休眠 1 秒
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
    }

    public static void main(String[] args) {
        MyThread thread1 = new MyThread();
        MyThread thread2 = new MyThread();

        thread1.start();  // 启动线程
        thread2.start();
    }
}
```

### 方式二：实现 Runnable

```java
public class MyRunnable implements Runnable {

    @Override
    public void run() {
        for (int i = 0; i < 5; i++) {
            System.out.println(Thread.currentThread().getName() + "：" + i);
            try {
                Thread.sleep(1000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
    }

    public static void main(String[] args) {
        MyRunnable runnable = new MyRunnable();

        Thread thread1 = new Thread(runnable, "线程A");
        Thread thread2 = new Thread(runnable, "线程B");

        thread1.start();
        thread2.start();
    }
}
```

### 方式三：实现 Callable（Java 5+）

```java
import java.util.concurrent.*;

public class MyCallable implements Callable<Integer> {

    @Override
    public Integer call() throws Exception {
        int sum = 0;
        for (int i = 1; i <= 100; i++) {
            sum += i;
        }
        return sum;
    }

    public static void main(String[] args) throws Exception {
        MyCallable callable = new MyCallable();

        // 使用 FutureTask
        FutureTask<Integer> futureTask = new FutureTask<>(callable);
        Thread thread = new Thread(futureTask);
        thread.start();

        // 获取结果
        Integer result = futureTask.get();
        System.out.println("计算结果：" + result);
    }
}
```

## 线程生命周期

```
新建 → 就绪 → 运行 → 阻塞 → 终止
  ↑               ↓
  └───────────────┘
```

### 线程状态

```java
public class ThreadState {

    public static void main(String[] args) throws InterruptedException {
        Thread thread = new Thread(() -> {
            try {
                Thread.sleep(1000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        });

        // NEW
        System.out.println("状态：" + thread.getState());  // NEW

        thread.start();
        // RUNNABLE
        System.out.println("状态：" + thread.getState());  // RUNNABLE

        Thread.sleep(500);
        // TIMED_WAITING
        System.out.println("状态：" + thread.getState());  // TIMED_WAITING

        thread.join();
        // TERMINATED
        System.out.println("状态：" + thread.getState());  // TERMINATED
    }
}
```

## Thread 常用方法

```java
public class ThreadMethods {

    public static void main(String[] args) {
        Thread thread = new Thread(() -> {
            for (int i = 0; i < 5; i++) {
                System.out.println(Thread.currentThread().getName() + "：" + i);
            }
        }, "我的线程");

        // 获取线程名称
        System.out.println("线程名：" + thread.getName());

        // 设置优先级（1-10）
        thread.setPriority(Thread.MAX_PRIORITY);

        // 判断线程是否存活
        System.out.println("存活：" + thread.isAlive());

        // 启动线程
        thread.start();

        // 让出 CPU
        Thread.yield();

        // 等待线程结束
        try {
            thread.join();
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
    }
}
```

## 实战演练

### 示例 1：多窗口卖票

```java
public class TicketSystem {

    private static int tickets = 100;

    public static void main(String[] args) {
        Runnable sellTask = () -> {
            while (tickets > 0) {
                System.out.println(Thread.currentThread().getName() + " 卖出第 " + tickets-- + " 张票");
                try {
                    Thread.sleep(100);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        };

        Thread window1 = new Thread(sellTask, "窗口1");
        Thread window2 = new Thread(sellTask, "窗口2");
        Thread window3 = new Thread(sellTask, "窗口3");

        window1.start();
        window2.start();
        window3.start();
    }
}
```

### 示例 2：生产者消费者

```java
import java.util.*;

public class ProducerConsumer {

    private static final int CAPACITY = 5;
    private static final Queue<Integer> queue = new LinkedList<>();

    public static void main(String[] args) {
        // 生产者
        Thread producer = new Thread(() -> {
            int value = 0;
            while (true) {
                synchronized (queue) {
                    while (queue.size() == CAPACITY) {
                        try {
                            queue.wait();
                        } catch (InterruptedException e) {
                            e.printStackTrace();
                        }
                    }
                    queue.offer(value++);
                    System.out.println("生产：" + (value - 1) + "，队列大小：" + queue.size());
                    queue.notifyAll();
                }
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        });

        // 消费者
        Thread consumer = new Thread(() -> {
            while (true) {
                synchronized (queue) {
                    while (queue.isEmpty()) {
                        try {
                            queue.wait();
                        } catch (InterruptedException e) {
                            e.printStackTrace();
                        }
                    }
                    int value = queue.poll();
                    System.out.println("消费：" + value + "，队列大小：" + queue.size());
                    queue.notifyAll();
                }
                try {
                    Thread.sleep(2000);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        });

        producer.start();
        consumer.start();
    }
}
```

### 示例 3：定时任务

```java
import java.util.*;

public class ScheduledTask {

    public static void main(String[] args) {
        Timer timer = new Timer();

        // 延迟执行
        timer.schedule(new TimerTask() {
            @Override
            public void run() {
                System.out.println("延迟任务执行");
            }
        }, 3000);  // 3 秒后执行

        // 周期执行
        timer.scheduleAtFixedRate(new TimerTask() {
            @Override
            public void run() {
                System.out.println("周期任务执行：" + new Date());
            }
        }, 1000, 2000);  // 1 秒后开始，每 2 秒执行一次
    }
}
```

## 最佳实践

### 1. 优先使用 Runnable

```java
// ✅ 好的做法：实现 Runnable
Runnable task = () -> { };
Thread thread = new Thread(task);

// ❌ 不好的做法：继承 Thread
class MyThread extends Thread { }
```

### 2. 使用线程池

```java
// ✅ 好的做法
ExecutorService executor = Executors.newFixedThreadPool(10);
executor.submit(task);

// ❌ 不好的做法：频繁创建线程
new Thread(task).start();
```

### 3. 正确处理中断

```java
public void run() {
    while (!Thread.currentThread().isInterrupted()) {
        // 执行任务
    }
}
```

## 常见问题

### Q1：run() 和 start() 的区别？

**答**：start() 启动线程并调用 run()，直接调用 run() 不会启动新线程。

### Q2：sleep() 和 wait() 的区别？

**答**：sleep() 是 Thread 方法，wait() 是 Object 方法，wait() 会释放锁。

### Q3：如何停止线程？

**答**：使用 interrupt() 和 isInterrupted() 判断，不推荐使用 stop()。

## 总结

本文学习了：

✅ **线程基础**

- 进程与线程
- 创建线程的三种方式

✅ **线程生命周期**

- 六种状态
- 状态转换

✅ **Thread 方法**

- start、run、sleep
- join、yield、interrupt

✅ **实战应用**

- 多窗口卖票
- 生产者消费者
- 定时任务

## 下一步

下一篇文章将学习线程同步。

---

- **上一篇**：[16 文件 IO 操作](./16-file-io.mdx)
- **下一篇**：[18 同步与并发工具](./18-synchronization.mdx)
