---
title: "12 异步任务与定时任务"
date: "2026-03-23"
summary: "掌握异步任务和定时任务的处理方式，实现后台任务调度。"
tags: ["SpringBoot", "异步任务", "定时任务", "线程池"]
category: "springboot"
cover: ""
draft: false
series: "SpringBoot 渐进式学习指南"
seriesOrder: 12
---

## 前言

异步任务和定时任务是 SpringBoot 开发中的常见需求。本文将讲解如何优雅地处理这些任务。

## 异步任务

### 启用异步

```java
@Configuration
@EnableAsync
public class AsyncConfig {
}
```

### @Async 注解

```java
@Service
public class EmailService {

    @Async
    public void sendEmail(String to, String subject, String content) {
        try {
            // 发送邮件逻辑
            Thread.sleep(5000);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        }
    }
}
```

### 自定义线程池

```java
@Configuration
@EnableAsync
public class AsyncConfig {

    @Bean(name = "taskExecutor")
    public Executor taskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(5);
        executor.setMaxPoolSize(10);
        executor.setQueueCapacity(100);
        executor.setThreadNamePrefix("async-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.initialize();
        return executor;
    }
}
```

### 指定线程池

```java
@Service
public class EmailService {

    @Async("taskExecutor")
    public void sendEmail(String to, String subject, String content) {
        // 使用指定的线程池
    }
}
```

## 定时任务

### 启用定时

```java
@Configuration
@EnableScheduling
public class ScheduledConfig {
}
```

### @Scheduled 注解

```java
@Component
public class ScheduledTasks {

    // 固定延迟执行
    @Scheduled(fixedRate = 5000)
    public void executeEvery5Seconds() {
        // 每5秒执行一次
    }

    // 固定延迟执行
    @Scheduled(fixedDelay = 5000)
    public void executeAfter5Seconds() {
        // 上一次执行完成后5秒再执行
    }

    // Cron 表达式
    @Scheduled(cron = "0 0 2 * * ?") // 每天凌晨2点
    public void executeDaily() {
        // 每天凌晨2点执行
    }
}
```

### Cron 表达式

```
字段：秒 分 时 日 月 周 年
*   *   *  *  *  *

示例：
0 0 2 * * ?        每天凌晨2点
0 0/15 * * * ?   每15分钟一次
0 0 8-18 * * ?   每天8点到18点，每2小时一次
0 0 0 * * ?       每天凌晨0点
```

## 异常处理

### Async 异常处理

```java
@Service
public class AsyncService {

    @Async
    public CompletableFuture<Void> asyncMethod() {
        return CompletableFuture.runAsync(() -> {
            // 异步逻辑
        });
    }
}
```

## 最佳实践

### 1. 异步方法不要返回值

```java
// ✅ 推荐：void 或 CompletableFuture
@Async
public void sendEmail(...) { }

@Async
public CompletableFuture<Result> asyncProcess(...) { }
```

### 2. 合理设置线程池大小

```java
// ✅ 推荐：根据任务类型设置
// CPU 密集型：核心数 + 1
// I/O 密集型：核心数 * 2
```

## 总结

本文介绍了异步任务和定时任务，主要内容：

- ✅ @Async 异步方法
- ✅ 自定义线程池
- ✅ @Scheduled 定时任务
- ✅ Cron 表达式

接下来我们将学习 WebSocket 实时通信。

## 下一步

在下一篇文章《13 WebSocket 实时通信》中，我们将学习：

- WebSocket 协议
- @EnableWebSocket
- WebSocketHandler
- STOMP 协议
- 广播与点对点消息
- 实时聊天室实现

---
