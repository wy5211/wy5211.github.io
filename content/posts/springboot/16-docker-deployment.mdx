---
title: "16 Docker 容器化部署"
date: "2026-03-23"
summary: "掌握 SpringBoot 应用的 Docker 容器化部署，实现应用的标准化交付。"
tags: ["SpringBoot", "Docker", "容器化", "部署"]
category: "springboot"
cover: ""
draft: false
series: "SpringBoot 渐进式学习指南"
seriesOrder: 16
---

## 前言

Docker 容器化是现代应用部署的标准方式。本文将讲解如何将 SpringBoot 应用容器化并部署。

## Docker 基础

### Dockerfile

```dockerfile
# 多阶段构建
FROM maven:3.9-eclipse-temurin-17 AS builder
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline
COPY src ./src
RUN mvn clean package -DskipTests

# 运行阶段
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY --from=builder /app/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

### 构建镜像

```bash
# 构建镜像
docker build -t springboot-app:1.0 .

# 运行容器
docker run -d -p 8080:8080 --name myapp springboot-app:1.0
```

## Docker Compose

### docker-compose.yml

```yaml
version: "3.8"

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/mydb
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=123456
    depends_on:
      - db
      - redis
    networks:
      - app-network

  db:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=123456
      - MYSQL_DATABASE=mydb
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - app-network

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - app-network

volumes:
  db-data:

networks:
  app-network:
    driver: bridge
```

### 启动服务

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f app

# 停止服务
docker-compose down

# 停止并删除数据
docker-compose down -v
```

## 最佳实践

### 1. 优化镜像大小

```dockerfile
# ✅ 推荐：使用 alpine 基础镜像
FROM eclipse-temurin:17-jre-alpine

# ❌ 避免：使用完整的 JDK 镜像
FROM eclipse-temurin:17
```

### 2. 多阶段构建

```dockerfile
# ✅ 推荐：多阶段构建减小镜像大小
FROM maven:3.9-eclipse-temurin-17 AS builder
# ... 构建步骤

FROM eclipse-temurin:17-jre-alpine
COPY --from=builder /app/target/*.jar app.jar
```

### 3. 健康检查

```dockerfile
HEALTHCHECK --interval=30s --timeout=3s \
  CMD curl -f http://localhost:8080/actuator/health || exit 1
```

## 生产环境配置

### application-prod.yml

```yaml
server:
  port: 8080

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  data:
    redis:
      host: ${REDIS_HOST}
      port: 6379

logging:
  level:
    root: INFO
  file:
    name: /app/logs/app.log
```

### 环境变量

```bash
# .env 文件
SPRING_PROFILES_ACTIVE=prod
SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/mydb
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=secure_password
REDIS_HOST=redis
```

## 总结

本文介绍了 Docker 容器化部署，主要内容：

- ✅ Dockerfile 编写
- ✅ Docker Compose 编排
- ✅ 多阶段构建
- ✅ 生产环境配置

接下来我们将学习性能优化。

## 下一步

在下一篇文章《17 性能优化》中，我们将学习：

- JVM 参数调优
- 数据库连接池优化
- 缓存策略优化
- 接口性能优化
- 监控与分析工具

---
