---
title: "18 微服务架构入门"
date: "2026-03-23"
summary: "了解微服务架构设计理念，掌握 Spring Cloud 核心组件的使用。"
tags: ["SpringBoot", "微服务", "Spring Cloud", "架构"]
category: "springboot"
cover: ""
draft: false
series: "SpringBoot 渐进式学习指南"
seriesOrder: 18
---

## 前言

微服务架构是现代企业级应用的主流架构模式。本文将介绍微服务的核心概念和 Spring Cloud 的使用。

## 微服务概述

### 什么是微服务

微服务架构是将单一应用拆分为多个小型服务的架构风格：

- **单一职责**：每个服务专注于一个业务功能
- **独立部署**：服务可以独立开发、部署和扩展
- **去中心化**：服务可以使用不同的技术栈
- **松耦合**：服务间通过 API 通信

### 单体应用 vs 微服务

| 特性     | 单体应用       | 微服务   |
| -------- | -------------- | -------- |
| 部署     | 整体部署       | 独立部署 |
| 扩展     | 整体扩展       | 按需扩展 |
| 技术栈   | 统一技术栈     | 灵活多样 |
| 复杂度   | 开发简单       | 运维复杂 |
| 故障隔离 | 牵一发而动全身 | 故障隔离 |

## Spring Cloud 介绍

### 核心组件

```
Spring Cloud = 多个组件的集合

┌─────────────────────────────────────┐
│         API Gateway (网关)          │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│    Service Discovery (注册中心)     │
│         Eureka / Nacos              │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│      Config Server (配置中心)       │
└─────────────────────────────────────┘
              ↓
┌──────────┬──────────┬──────────┐
│ Service A │ Service B │ Service C │
└──────────┴──────────┴──────────┘
```

## 服务注册与发现

### Eureka Server

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-netflix-eureka-server</artifactId>
</dependency>
```

### 启动 Eureka Server

```java
@SpringBootApplication
@EnableEurekaServer
public class EurekaServerApplication {
    public static void main(String[] args) {
        SpringApplication.run(EurekaServerApplication.class, args);
    }
}
```

### Eureka 配置

```yaml
server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
```

### 服务注册

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
</dependency>
```

```yaml
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
```

## 服务调用

### RestTemplate

```java
@Configuration
public class RestTemplateConfig {

    @Bean
    @LoadBalanced
    public RestTemplate restTemplate() {
        return new RestTemplate();
    }
}

@Service
public class UserService {

    @Autowired
    private RestTemplate restTemplate;

    public Order getUserOrder(Long userId) {
        // 使用服务名调用
        return restTemplate.getForObject(
            "http://order-service/orders/" + userId,
            Order.class
        );
    }
}
```

### OpenFeign

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-openfeign</artifactId>
</dependency>
```

```java
@SpringBootApplication
@EnableFeignClients
public class UserServiceApplication {
    // ...
}

@FeignClient(name = "order-service")
public interface OrderClient {

    @GetMapping("/orders/{userId}")
    Order getOrderByUserId(@PathVariable("userId") Long userId);
}

@Service
public class UserService {

    @Autowired
    private OrderClient orderClient;

    public Order getUserOrder(Long userId) {
        return orderClient.getOrderByUserId(userId);
    }
}
```

## 配置中心

### Config Server

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-config-server</artifactId>
</dependency>
```

```java
@SpringBootApplication
@EnableConfigServer
public class ConfigServerApplication {
    // ...
}
```

```yaml
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-repo/configs
```

### Config Client

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-config</artifactId>
</dependency>
```

```yaml
spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
```

## 服务网关

### Gateway 搭建

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-gateway</artifactId>
</dependency>
```

### 路由配置

```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/users/**
          filters:
            - StripPrefix=1

        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/api/orders/**
          filters:
            - StripPrefix=1
```

## 微服务最佳实践

### 1. 服务拆分原则

```java
// ✅ 推荐：按业务领域拆分
- 用户服务（用户、权限）
- 订单服务（订单、支付）
- 商品服务（商品、库存）

// ❌ 避免：按技术层拆分
- Service 层服务
- DAO 层服务
```

### 2. 数据库分离

```java
// ✅ 推荐：每个服务独立数据库
用户服务 → user_db
订单服务 → order_db

// ❌ 避免：多个服务共享数据库
```

### 3. 幂等性设计

```java
@RestController
public class OrderController {

    @PostMapping("/orders")
    public Result<Order> createOrder(@RequestHeader("idempotent-key") String key) {
        // 使用幂等键防止重复提交
        return orderService.createOrder(key);
    }
}
```

## 总结

本文介绍了微服务架构入门，主要内容：

- ✅ 微服务架构概念
- ✅ Spring Cloud 组件
- ✅ 服务注册与发现
- ✅ 服务调用方式
- ✅ 配置中心
- ✅ 服务网关

## 系列总结

恭喜你完成了《SpringBoot 渐进式学习指南》的全部 18 篇文章！

你已经学习了：

1. **基础篇**（01-03）：环境搭建、第一个应用、依赖注入
2. **核心篇**（04-08）：RESTful API、数据访问、参数校验、异常处理、AOP
3. **安全篇**（09-10）：Spring Security、JWT 认证
4. **进阶篇**（11-15）：Redis 缓存、异步任务、WebSocket、日志监控、测试
5. **部署篇**（16-18）：Docker 部署、性能优化、微服务架构

### 下一步学习建议

- **深入源码**：阅读 SpringBoot 源码，理解自动配置原理
- **实战项目**：开发一个完整的微服务项目
- **性能调优**：深入学习 JVM 调优和性能分析
- **云原生**：学习 Kubernetes 和云原生技术栈
- **分布式技术**：消息队列、分布式事务、分布式锁

### 推荐资源

- 官方文档：https://spring.io/projects/spring-boot
- Spring Cloud：https://spring.io/projects/spring-cloud
- 社区论坛：https://stackoverflow.com/questions/tagged/spring-boot

祝你在 SpringBoot 学习之路上越走越远！

---

- **上一篇**：[17 性能优化](./17-performance-optimization.mdx)
