---
title: "23 实战：Web 服务器"
date: "2026-03-30"
summary: "使用 Axum 框架构建一个完整的 RESTful API 服务器，综合运用所学知识"
tags: ["Rust", "Axum", "Web", "RESTful", "实战"]
category: "rust"
draft: false
series: "Rust 渐进式学习"
seriesOrder: 23
---

## 前言

学完了 Rust 的核心概念，是时候实战了！我们将使用 **Axum** 框架构建一个 RESTful API 服务器——一个简单的用户管理系统。

Axum 是 Tokio 团队开发的 Web 框架，具有以下特点：

- 类型安全：编译器帮你检查路由和参数
- 高性能：基于 Tokio 和 Hyper
- 简洁的 API：直观的路由定义
- 生态丰富：与 Tower 中间件生态兼容

## 项目搭建

### 创建项目

```bash
cargo new user-api
cd user-api
```

### 添加依赖

```toml
# Cargo.toml
[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
```

## 基本服务器

```rust
// src/main.rs
use axum::{routing::get, Router};

async fn hello() -> &'static str {
    "Hello, Axum!"
}

#[tokio::main]
async fn main() {
    let app = Router::new()
        .route("/", get(hello));

    let listener = tokio::net::TcpListener::bind("0.0.0.0:3000").await.unwrap();
    println!("服务器运行在 http://localhost:3000");
    axum::serve(listener, app).await.unwrap();
}
```

运行：`cargo run`，访问 `http://localhost:3000`。

## 路由和处理器

### 定义路由

```rust
use axum::{
    routing::{get, post, put, delete},
    Router,
};

let app = Router::new()
    .route("/", get(root))
    .route("/users", get(list_users).post(create_user))
    .route("/users/:id", get(get_user).put(update_user).delete(delete_user));
```

### 路径参数

```rust
use axum::extract::Path;

async fn get_user(Path(id): Path<u64>) -> String {
    format!("获取用户: {}", id)
}
```

### 查询参数

```rust
use axum::extract::Query;
use serde::Deserialize;

#[derive(Deserialize)]
struct Pagination {
    page: Option<usize>,
    per_page: Option<usize>,
}

async fn list_users(Query(pagination): Query<Pagination>) -> String {
    let page = pagination.page.unwrap_or(1);
    let per_page = pagination.per_page.unwrap_or(10);
    format!("第 {} 页，每页 {} 条", page, per_page)
}
```

## JSON 处理

### 请求和响应

```rust
use axum::{Json, extract::State};
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Clone, Debug)]
struct User {
    id: u64,
    username: String,
    email: String,
}

#[derive(Deserialize)]
struct CreateUser {
    username: String,
    email: String,
}

// 创建用户（JSON 请求体）
async fn create_user(Json(input): Json<CreateUser>) -> Json<User> {
    Json(User {
        id: 1,
        username: input.username,
        email: input.email,
    })
}

// 获取用户列表（JSON 响应）
async fn list_users() -> Json<Vec<User>> {
    Json(vec![
        User { id: 1, username: "alice".into(), email: "a@b.com".into() },
        User { id: 2, username: "bob".into(), email: "b@c.com".into() },
    ])
}
```

## 状态共享

使用 `State` 在处理器之间共享数据：

```rust
use axum::extract::State;
use std::sync::{Arc, Mutex};

type AppState = Arc<Mutex<Vec<User>>>;

async fn list_users(State(state): State<AppState>) -> Json<Vec<User>> {
    let users = state.lock().unwrap();
    Json(users.clone())
}

async fn create_user(
    State(state): State<AppState>,
    Json(input): Json<CreateUser>,
) -> Json<User> {
    let mut users = state.lock().unwrap();
    let id = users.len() as u64 + 1;
    let user = User {
        id,
        username: input.username,
        email: input.email,
    };
    users.push(user.clone());
    Json(user)
}

async fn delete_user(
    State(state): State<AppState>,
    Path(id): Path<u64>,
) -> Result<Json<User>, StatusCode> {
    let mut users = state.lock().unwrap();
    let index = users.iter().position(|u| u.id == id);

    match index {
        Some(i) => {
            let user = users.remove(i);
            Ok(Json(user))
        }
        None => Err(StatusCode::NOT_FOUND),
    }
}
```

## 错误处理

```rust
use axum::{
    http::StatusCode,
    response::IntoResponse,
    Json,
};
use serde_json::json;

enum AppError {
    NotFound(String),
    BadRequest(String),
}

impl IntoResponse for AppError {
    fn into_response(self) -> axum::response::Response {
        let (status, message) = match self {
            AppError::NotFound(msg) => (StatusCode::NOT_FOUND, msg),
            AppError::BadRequest(msg) => (StatusCode::BAD_REQUEST, msg),
        };

        let body = Json(json!({ "error": message }));
        (status, body).into_response()
    }
}
```

## 完整示例：用户 CRUD API

```rust
use axum::{
    extract::{Path, State},
    http::StatusCode,
    routing::{delete, get, post, put},
    Json, Router,
};
use serde::{Deserialize, Serialize};
use std::sync::{Arc, Mutex};

#[derive(Serialize, Deserialize, Clone, Debug)]
struct User {
    id: u64,
    username: String,
    email: String,
}

#[derive(Deserialize)]
struct CreateUser {
    username: String,
    email: String,
}

#[derive(Deserialize)]
struct UpdateUser {
    username: Option<String>,
    email: Option<String>,
}

type Db = Arc<Mutex<Vec<User>>>;

async fn list_users(State(db): State<Db>) -> Json<Vec<User>> {
    let users = db.lock().unwrap();
    Json(users.clone())
}

async fn get_user(
    State(db): State<Db>,
    Path(id): Path<u64>,
) -> Result<Json<User>, StatusCode> {
    let users = db.lock().unwrap();
    users.iter()
        .find(|u| u.id == id)
        .cloned()
        .map(Json)
        .ok_or(StatusCode::NOT_FOUND)
}

async fn create_user(
    State(db): State<Db>,
    Json(input): Json<CreateUser>,
) -> (StatusCode, Json<User>) {
    let mut users = db.lock().unwrap();
    let id = users.last().map(|u| u.id + 1).unwrap_or(1);
    let user = User {
        id,
        username: input.username,
        email: input.email,
    };
    users.push(user.clone());
    (StatusCode::CREATED, Json(user))
}

async fn update_user(
    State(db): State<Db>,
    Path(id): Path<u64>,
    Json(input): Json<UpdateUser>,
) -> Result<Json<User>, StatusCode> {
    let mut users = db.lock().unwrap();
    let user = users.iter_mut().find(|u| u.id == id).ok_or(StatusCode::NOT_FOUND)?;

    if let Some(username) = input.username {
        user.username = username;
    }
    if let Some(email) = input.email {
        user.email = email;
    }

    Ok(Json(user.clone()))
}

async fn delete_user(
    State(db): State<Db>,
    Path(id): Path<u64>,
) -> StatusCode {
    let mut users = db.lock().unwrap();
    if users.iter().any(|u| u.id == id) {
        users.retain(|u| u.id != id);
        StatusCode::NO_CONTENT
    } else {
        StatusCode::NOT_FOUND
    }
}

#[tokio::main]
async fn main() {
    let db: Db = Arc::new(Mutex::new(Vec::new()));

    let app = Router::new()
        .route("/users", get(list_users).post(create_user))
        .route("/users/:id", get(get_user).put(update_user).delete(delete_user))
        .with_state(db);

    let listener = tokio::net::TcpListener::bind("0.0.0.0:3000").await.unwrap();
    println!("用户 API 运行在 http://localhost:3000");
    axum::serve(listener, app).await.unwrap();
}
```

### 测试 API

```bash
# 创建用户
curl -X POST http://localhost:3000/users \
  -H "Content-Type: application/json" \
  -d '{"username": "alice", "email": "alice@example.com"}'

# 获取用户列表
curl http://localhost:3000/users

# 获取单个用户
curl http://localhost:3000/users/1

# 更新用户
curl -X PUT http://localhost:3000/users/1 \
  -H "Content-Type: application/json" \
  -d '{"email": "new@example.com"}'

# 删除用户
curl -X DELETE http://localhost:3000/users/1
```

## 最佳实践

1. **使用 `State` 共享数据**：不要用全局变量
2. **错误统一处理**：实现 `IntoResponse` trait
3. **输入验证**：使用 `validator` crate
4. **日志**：使用 `tracing` crate
5. **配置管理**：使用 `config` crate

## 总结

本文通过实战学习了 Axum Web 框架：

**核心概念**

- 路由定义：`Router::new().route()`
- 处理器：异步函数 + 提取器
- JSON 处理：`Json<T>` 提取器
- 状态共享：`State<Arc<Mutex<T>>>`

**CRUD 实现**

- 列表、创建、查询、更新、删除
- 错误处理和状态码

## 下一步

最后一篇文章将学习如何开发一个**命令行工具（CLI）**，完成 Rust 学习之旅！

---

- **上一篇**：[22 与 C 语言交互](./22-ffi.mdx)
- **下一篇**：[24 实战：命令行工具](./24-cli-tool.mdx)
