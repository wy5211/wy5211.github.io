---
title: "03 Docker 容器化全栈应用部署"
date: "2026-03-18"
summary: "深入探讨 Docker 容器化的核心概念、多阶段构建优化、Next.js 应用的 Docker 化、数据库容器化以及从开发到生产的完整部署流程。"
tags: ["Docker", "容器化", "部署", "DevOps", "全栈开发"]
category: "运维"
cover: ""
draft: false
---

# Docker 容器化全栈应用部署

在现代软件开发中，容器化已经成为了标准。Docker 作为最流行的容器化平台，能够确保应用在任何环境中都能以相同的方式运行，极大简化了从开发到部署的流程。

本文将深入探讨 Docker 容器化的核心概念，以及如何将全栈应用从开发环境部署到生产环境。

## Docker 核心概念

### 什么是容器？

容器是一种轻量级的虚拟化技术，它将应用程序及其所有依赖项打包在一起，确保在任何环境中都能以相同的方式运行。

**容器 vs 虚拟机**：

```
虚拟机：
┌─────────────────────────────────────┐
│       虚拟机监视器（Hypervisor）     │
│  ┌────────────┐  ┌────────────┐ │
│  │  操作系统   │  │  操作系统   │ │
│  │ ┌────────┐│  │ ┌────────┐│ │
│  │ │应用 A  ││  │ │应用 B  ││ │
│  │ └────────┘│  │ └────────┘│ │
│  └────────────┘  └────────────┘ │
└─────────────────────────────────────┘
每个虚拟机都有完整的操作系统

容器：
┌─────────────────────────────────────┐
│         Docker 守护进程           │
│  ┌───────────┐  ┌───────────┐ │
│  │ 容器 A    │  │ 容器 B    │ │
│  │ ┌─────┐  │  │ ┌─────┐  │ │
│  │ │应用  │  │  │ │应用  │  │ │
│  │ └─────┘  │  │ └─────┘  │ │
│  └───────────┘  └───────────┘ │
└─────────────────────────────────────┘
容器共享主机操作系统内核
```

### Docker 核心组件

1. **Dockerfile**：定义如何构建镜像的文本文件
2. **Docker Image**：只读模板，包含应用和依赖
3. **Docker Container**：镜像的运行实例
4. **Docker Registry**：存储和分发镜像的地方（如 Docker Hub）

```bash
# Dockerfile 示例
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

### Docker 命令基础

```bash
# 构建镜像
docker build -t my-app:1.0 .

# 运行容器
docker run -p 3000:3000 my-app:1.0

# 后台运行
docker run -d -p 3000:3000 my-app:1.0

# 查看运行中的容器
docker ps

# 查看所有容器（包括停止的）
docker ps -a

# 停止容器
docker stop <container_id>

# 删除容器
docker rm <container_id>

# 删除镜像
docker rmi my-app:1.0

# 查看容器日志
docker logs <container_id>

# 进入容器
docker exec -it <container_id> /bin/sh
```

## 多阶段构建优化

多阶段构建是减少 Docker 镜像大小的关键技术。通过分离构建环境和运行环境，可以只保留运行时需要的文件。

### 基础多阶段构建

```dockerfile
# 第一阶段：构建
FROM node:18-alpine AS builder

WORKDIR /app

# 复制依赖文件
COPY package.json package-lock.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 构建应用
RUN npm run build

# 第二阶段：运行
FROM node:18-alpine

WORKDIR /app

# 从构建阶段复制构建产物
COPY --from=builder /app/.next ./.next
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/package.json ./

# 安装生产依赖
RUN npm ci --only=production

# 暴露端口
EXPOSE 3000

# 设置环境变量
ENV NODE_ENV=production

# 启动应用
CMD ["node", "server.js"]
```

### 优化版多阶段构建

```dockerfile
# 第一阶段：依赖安装
FROM node:18-alpine AS deps
WORKDIR /app

# 只复制依赖文件，利用 Docker 缓存
COPY package.json package-lock.json ./
RUN npm ci

# 第二阶段：构建
FROM node:18-alpine AS builder
WORKDIR /app

# 从依赖阶段复制 node_modules
COPY --from=deps /app/node_modules ./node_modules

# 复制源代码
COPY . .

# 构建应用
RUN npm run build

# 第三阶段：生产环境
FROM node:18-alpine AS production
WORKDIR /app

# 从构建阶段复制必要的文件
COPY --from=builder /app/package.json ./
COPY --from=builder /app/.next ./.next

# 从依赖阶段复制生产依赖
COPY --from=deps /app/node_modules ./node_modules

# 创建非 root 用户
RUN addgroup -g 1001 -S nodejs
RUN adduser -S nextjs -u 1001
RUN chown -R nextjs:nodejs /app
USER nextjs

# 暴露端口
EXPOSE 3000

# 环境变量
ENV NODE_ENV=production
ENV PORT=3000

# 健康检查
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD node healthcheck.js || exit 1

# 启动应用
CMD ["node", "server.js"]
```

### 镜像大小对比

```bash
# 单阶段构建（未优化）
REPOSITORY          TAG       SIZE
my-app             1.0       1.2GB

# 多阶段构建（优化后）
REPOSITORY          TAG       SIZE
my-app             1.0       180MB
```

## Next.js 应用的 Docker 化

### 基础 Next.js Dockerfile

```dockerfile
# Dockerfile
FROM node:18-alpine AS base

# 安装依赖阶段
FROM base AS deps
RUN apk add --no-cache libc6-compat
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

# 构建阶段
FROM base AS builder
WORKDIR /app

COPY --from=deps /app/node_modules ./node_modules
COPY . .

# 禁用遥测
ENV NEXT_TELEMETRY_DISABLED 1

RUN npm run build

# 生产运行阶段
FROM base AS runner
WORKDIR /app

ENV NODE_ENV production
ENV NEXT_TELEMETRY_DISABLED 1

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

# 复制必要文件
COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next ./.next
COPY --from=builder --chown=nextjs:nodejs /app/node_modules ./node_modules
COPY --from=builder --chown=nextjs:nodejs /app/package.json ./

USER nextjs

EXPOSE 3000

ENV PORT 3000
ENV HOSTNAME "0.0.0.0"

CMD ["npm", "start"]
```

### Standalone 模式（推荐）

Next.js 的 Standalone 模式可以生成一个独立的服务器文件，大幅减少镜像大小。

```javascript
// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone", // 启用 standalone 模式
  experimental: {
    // 可选：减少包大小
    optimizePackageImports: ["lucide-react", "@heroicons/react"],
  },
};

module.exports = nextConfig;
```

```dockerfile
# 使用 standalone 模式的 Dockerfile
FROM node:18-alpine AS base

# 依赖安装
FROM base AS deps
RUN apk add --no-cache libc6-compat
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

# 构建
FROM base AS builder
WORKDIR /app

COPY --from=deps /app/node_modules ./node_modules
COPY . .

ENV NEXT_TELEMETRY_DISABLED 1

RUN npm run build

# 生产运行
FROM base AS runner
WORKDIR /app

ENV NODE_ENV production
ENV NEXT_TELEMETRY_DISABLED 1

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

# 只复制 standalone 输出和 public 文件
COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

USER nextjs

EXPOSE 3000

ENV PORT 3000
ENV HOSTNAME "0.0.0.0"

CMD ["node", "server.js"]
```

### .dockerignore 文件

```dockerignore
# .dockerignore
node_modules
.next
.git
.gitignore
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.md
```

## 数据库容器化

### PostgreSQL

```dockerfile
# Dockerfile（如果需要自定义 PostgreSQL）
FROM postgres:15-alpine

# 初始化脚本
COPY init.sql /docker-entrypoint-initdb.d/

# 配置
ENV POSTGRES_USER=app
ENV POSTGRES_PASSWORD=secret
ENV POSTGRES_DB=myapp

EXPOSE 5432
```

```yaml
# docker-compose.yml
version: "3.8"

services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: myapp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d myapp"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
```

### MongoDB

```yaml
version: "3.8"

services:
  mongodb:
    image: mongo:7
    container_name: mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: secret
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongo localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mongodb_data:
```

### Redis

```yaml
version: "3.8"

services:
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass secret
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  redis_data:
```

## Docker Compose 开发环境

Docker Compose 可以定义和运行多容器 Docker 应用，非常适合本地开发环境。

### 完整的开发环境

```yaml
# docker-compose.yml
version: "3.8"

services:
  # 应用
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://app:secret@postgres:5432/myapp
      - REDIS_URL=redis://redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev

  # 数据库
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: myapp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d myapp"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # pgAdmin（数据库管理）
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: secret
      PGADMIN_CONFIG_SERVER_MODE_NAME: main
    ports:
      - "5050:80"
    depends_on:
      - postgres

  # Redis Commander（Redis 管理）
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: redis-commander
    restart: unless-stopped
    environment:
      REDIS_HOSTS: local:redis:6379
    ports:
      - "8081:8081"
    depends_on:
      - redis

volumes:
  postgres_data:
  redis_data:
```

### 开发环境使用

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f app

# 重启服务
docker-compose restart app

# 停止所有服务
docker-compose down

# 停止并删除数据卷
docker-compose down -v

# 重新构建镜像
docker-compose build --no-cache

# 只启动特定服务
docker-compose up postgres redis

# 执行命令
docker-compose exec app npm run test
docker-compose exec postgres psql -U app -d myapp
```

### 开发环境 Dockerfile

```dockerfile
# Dockerfile.dev（开发环境专用）
FROM node:18-alpine

WORKDIR /app

# 安装所有依赖（包括 devDependencies）
COPY package.json package-lock.json ./
RUN npm ci

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 开发模式
ENV NODE_ENV=development

# 启动开发服务器
CMD ["npm", "run", "dev"]
```

```yaml
# docker-compose.dev.yml（开发环境）
version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: nextjs-app-dev
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules # 防止本地 node_modules 覆盖容器内的
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://app:secret@postgres:5432/myapp
      - REDIS_URL=redis://redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: postgres:15-alpine
    container_name: postgres-dev
    restart: unless-stopped
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: myapp
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d myapp"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_dev_data:
```

## 生产部署策略

### 构建生产镜像

```bash
# 构建生产镜像
docker build -t my-registry.com/myapp:latest .

# 推送到镜像仓库
docker push my-registry.com/myapp:latest
```

### 使用 GitHub Actions 构建和推送

```yaml
# .github/workflows/docker.yml
name: Docker Build and Push

on:
  push:
    branches: [main]
    tags: ["v*"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Registry
        uses: docker/login-action@v2
        with:
          registry: my-registry.com
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: my-registry.com/myapp
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

### Docker Registry 选择

| Registry                  | 特点              | 适用场景    |
| ------------------------- | ----------------- | ----------- |
| Docker Hub                | 公开、免费        | 开源项目    |
| GitHub Container Registry | GitHub 集成       | GitHub 项目 |
| GitLab Container Registry | GitLab 集成       | GitLab 项目 |
| AWS ECR                   | AWS 集成          | AWS 部署    |
| Google GCR                | Google Cloud 集成 | GCP 部署    |
| Azure ACR                 | Azure 集成        | Azure 部署  |
| Harbor                    | 私有、企业级      | 企业内部    |

### 云平台部署

#### Vercel（推荐）

```bash
# 使用 Vercel CLI
npm i -g vercel

# 登录
vercel login

# 部署
vercel
```

Vercel 支持自动从 GitHub 部署，配置 `.vercelignore` 和 `vercel.json` 即可。

#### Railway

```yaml
# railway.toml
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

[[services]]
name = "web"
env = ["NODE_ENV=production"]
```

#### 自托管服务器

```yaml
# docker-compose.prod.yml
version: "3.8"

services:
  app:
    image: my-registry.com/myapp:latest
    container_name: nextjs-app-prod
    restart: always
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://app:${DB_PASSWORD}@postgres:5432/myapp
      - REDIS_URL=redis://redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - app-network

  postgres:
    image: postgres:15-alpine
    container_name: postgres-prod
    restart: always
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password
      POSTGRES_DB: myapp
    secrets:
      - db_password
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d myapp"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network

  redis:
    image: redis:7-alpine
    container_name: redis-prod
    restart: always
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    secrets:
      - redis_password
    volumes:
      - redis_prod_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  postgres_prod_data:
  redis_prod_data:
```

```bash
# 创建 secrets
echo "your_db_password" > db_password.txt
echo "your_redis_password" > redis_password.txt

# 部署
docker-compose -f docker-compose.prod.yml --env-file .env up -d
```

### 使用 Docker Swarm

```yaml
# docker-compose.swarm.yml
version: "3.8"

services:
  app:
    image: my-registry.com/myapp:latest
    deploy:
      replicas: 3
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
        order: start-first
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
    networks:
      - app-network

networks:
  app-network:
    driver: overlay
```

```bash
# 初始化 Swarm
docker swarm init

# 部署
docker stack deploy -c docker-compose.swarm.yml myapp

# 查看服务
docker service ls

# 扩展服务
docker service scale myapp_app=5

# 更新服务
docker service update myapp_app --image my-registry.com/myapp:v2
```

### 使用 Kubernetes

```yaml
# k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextjs-app
  labels:
    app: nextjs-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nextjs-app
  template:
    metadata:
      labels:
        app: nextjs-app
    spec:
      containers:
        - name: nextjs-app
          image: my-registry.com/myapp:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: database-url
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: nextjs-app
spec:
  selector:
    app: nextjs-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer

---
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  database-url: postgresql://app:secret@postgres:5432/myapp
```

```bash
# 部署到 Kubernetes
kubectl apply -f k8s/deployment.yaml

# 查看状态
kubectl get pods
kubectl get services

# 查看日志
kubectl logs -f deployment/nextjs-app

# 扩展
kubectl scale deployment nextjs-app --replicas=5

# 更新镜像
kubectl set image deployment/nextjs-app nextjs-app=my-registry.com/myapp:v2
```

## 监控和日志

### 健康检查

```typescript
// app/api/health/route.ts
import { NextResponse } from "next/server";

export async function GET() {
  // 检查数据库连接
  try {
    await prisma.$queryRaw`SELECT 1`;
  } catch (error) {
    return NextResponse.json(
      { status: "unhealthy", error: "Database connection failed" },
      { status: 503 }
    );
  }

  // 检查 Redis 连接
  try {
    await redis.ping();
  } catch (error) {
    return NextResponse.json(
      { status: "unhealthy", error: "Redis connection failed" },
      { status: 503 }
    );
  }

  return NextResponse.json({
    status: "healthy",
    timestamp: new Date().toISOString(),
    uptime: process.uptime(),
  });
}
```

### 日志收集

```yaml
# docker-compose.yml（使用日志驱动）
version: "3.8"

services:
  app:
    image: my-registry.com/myapp:latest
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

```yaml
# 使用 Loki 收集日志
version: "3.8"

services:
  app:
    image: my-registry.com/myapp:latest
    logging:
      driver: loki
      options:
        loki-url: "http://localhost:3100/loki/api/v1/push"
        loki-batch-size: "400"

  loki:
    image: grafana/loki:latest
    ports:
      - "3100:3100"
    volumes:
      - loki_data:/loki

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=secret
    volumes:
      - grafana_data:/var/lib/grafana

volumes:
  loki_data:
  grafana_data:
```

## 最佳实践

### 1. 安全性

```dockerfile
# Dockerfile 安全最佳实践
FROM node:18-alpine

# 使用非 root 用户
RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

# 只复制必要文件
COPY --chown=nextjs:nodejs package.json ./
RUN npm ci --production

# 设置权限
USER nextjs

# 只暴露必要端口
EXPOSE 3000

# 健康检查
HEALTHCHECK --interval=30s --timeout=3s \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1

# 扫描漏洞
# docker scan myapp:latest
```

### 2. 缓存优化

```dockerfile
# 利用 Docker 缓存层
FROM node:18-alpine AS base

# 1. 复制依赖文件（变化频率低）
COPY package.json package-lock.json ./

# 2. 安装依赖（利用缓存）
RUN npm ci

# 3. 复制源代码（变化频率高）
COPY . .

# 4. 构建
RUN npm run build
```

### 3. 镜像大小优化

```bash
# 分析镜像大小
docker history myapp:latest

# 查看每一层的大小

# 使用 Alpine 镜像
FROM node:18-alpine  # 而不是 node:18

# 多阶段构建
# 只复制必要的文件到最终镜像

# 清理缓存
RUN npm ci --production && npm cache clean --force
```

### 4. 环境变量管理

```yaml
# docker-compose.yml
version: "3.8"

services:
  app:
    image: my-registry.com/myapp:latest
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - DATABASE_URL=${DATABASE_URL}
```

```bash
# .env
NODE_ENV=production
DATABASE_URL=postgresql://app:secret@postgres:5432/myapp
REDIS_URL=redis://redis:6379

# .env.example（提交到 Git）
NODE_ENV=
DATABASE_URL=
REDIS_URL=
```

## 总结

Docker 容器化已经成为现代应用部署的标准。通过合理的镜像设计、多阶段构建和部署策略，可以构建高效、可维护的全栈应用。

### 核心要点

1. **多阶段构建**：大幅减少镜像大小
2. **Standalone 模式**：Next.js 生产环境推荐
3. **Docker Compose**：简化本地开发环境
4. **健康检查**：确保应用可用性
5. **安全实践**：使用非 root 用户、扫描漏洞

### 最佳实践

- 使用多阶段构建优化镜像大小
- 利用 Docker 缓存加速构建
- 使用 Alpine 基础镜像
- 实现健康检查端点
- 使用非 root 用户运行容器
- 合理管理环境变量和 secrets
- 配置日志收集和监控

容器化不仅仅是一种技术选择，更是现代 DevOps 文化的基础。掌握 Docker 将极大提升你的开发和部署效率。
