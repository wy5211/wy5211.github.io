---
title: "09. JWT 认证与授权"
date: "2026-03-29"
summary: "使用 TypeORM + NestJS + Passport 实现完整的认证授权系统，包含用户注册登录、JWT Token 管理、角色权限和资源级访问控制。"
tags: ["TypeORM", "JWT", "认证", "授权", "NestJS", "Passport"]
category: "typeorm"
draft: false
series: "TypeORM 实战：NestJS 数据库开发"
seriesOrder: 9
---

## 前言

认证（Authentication）和授权（Authorization）是几乎所有后端应用的核心安全模块。本篇将结合 TypeORM 和 NestJS，从零实现一个完整的 JWT 认证授权系统。

**本篇你将学到**：

- User 和 RefreshToken 实体设计
- 密码加密（bcrypt）
- Passport + JWT 策略
- 注册 / 登录 / 刷新 Token
- 角色权限（@Roles 装饰器 + Guard）
- 资源级访问控制

---

## 安装依赖

```bash
# 认证相关
npm install @nestjs/passport passport passport-jwt
npm install @types/passport-jwt -D

# 密码加密
npm install bcrypt
npm install @types/bcrypt -D
```

---

## 实体设计

### User 实体

```typescript
// src/modules/auth/entities/user.entity.ts
import {
  Entity,
  PrimaryGeneratedColumn,
  Column,
  CreateDateColumn,
  UpdateDateColumn,
  DeleteDateColumn,
  OneToMany,
  BeforeInsert,
  BeforeUpdate,
  Index,
} from "typeorm";
import * as bcrypt from "bcrypt";
import { RefreshToken } from "./refresh-token.entity";

export enum UserRole {
  ADMIN = "ADMIN",
  EDITOR = "EDITOR",
  AUTHOR = "AUTHOR",
  READER = "READER",
}

@Entity("users")
@Index(["email"], { unique: true })
@Index(["username"], { unique: true })
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ length: 100 })
  name: string;

  @Column({ unique: true, length: 50 })
  username: string;

  @Column({ unique: true, length: 255 })
  email: string;

  @Column({ name: "password_hash", select: false })
  passwordHash: string;

  @Column({ nullable: true, length: 500 })
  avatar: string;

  @Column({ type: "text", nullable: true })
  bio: string;

  @Column({
    type: "enum",
    enum: UserRole,
    default: UserRole.READER,
  })
  role: UserRole;

  @Column({ default: true })
  isActive: boolean;

  @OneToMany(() => RefreshToken, (token) => token.user)
  refreshTokens: RefreshToken[];

  @CreateDateColumn({ name: "created_at" })
  createdAt: Date;

  @UpdateDateColumn({ name: "updated_at" })
  updatedAt: Date;

  @DeleteDateColumn({ name: "deleted_at" })
  deletedAt: Date;

  // 密码管理
  private _password: string;

  set password(value: string) {
    this._password = value;
  }

  @BeforeInsert()
  @BeforeUpdate()
  async hashPassword() {
    if (this._password) {
      this.passwordHash = await bcrypt.hash(this._password, 10);
    }
  }

  async validatePassword(password: string): Promise<boolean> {
    return bcrypt.compare(password, this.passwordHash);
  }
}
```

### RefreshToken 实体

```typescript
// src/modules/auth/entities/refresh-token.entity.ts
import {
  Entity,
  PrimaryGeneratedColumn,
  Column,
  CreateDateColumn,
  ManyToOne,
  JoinColumn,
} from "typeorm";
import { User } from "./user.entity";

@Entity("refresh_tokens")
export class RefreshToken {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ name: "user_id" })
  userId: number;

  @ManyToOne(() => User, (user) => user.refreshTokens, {
    onDelete: "CASCADE",
  })
  @JoinColumn({ name: "user_id" })
  user: User;

  @Column({ unique: true, length: 500 })
  token: string;

  @Column({ name: "expires_at", type: "timestamptz" })
  expiresAt: Date;

  @Column({ default: false })
  revoked: boolean;

  @CreateDateColumn({ name: "created_at" })
  createdAt: Date;

  // 检查是否过期
  get isExpired(): boolean {
    return new Date() > this.expiresAt;
  }

  // 检查是否有效
  get isValid(): boolean {
    return !this.revoked && !this.isExpired;
  }
}
```

---

## JWT 策略

### JWT 配置

```typescript
// src/modules/auth/constants.ts
export const jwtConstants = {
  secret: process.env.JWT_SECRET || "your-secret-key",
  accessTokenExpiration: "15m", // 访问令牌 15 分钟
  refreshTokenExpiration: "7d", // 刷新令牌 7 天
};
```

### JWT Strategy

```typescript
// src/modules/auth/strategies/jwt.strategy.ts
import { ExtractJwt, Strategy } from "passport-jwt";
import { PassportStrategy } from "@nestjs/passport";
import { InjectRepository } from "@nestjs/typeorm";
import { Repository } from "typeorm";
import { UnauthorizedException } from "@nestjs/common";
import { User } from "../entities/user.entity";
import { jwtConstants } from "../constants";

interface JwtPayload {
  sub: number;
  username: string;
  role: string;
}

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(
    @InjectRepository(User)
    private userRepo: Repository<User>
  ) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: jwtConstants.secret,
    });
  }

  async validate(payload: JwtPayload) {
    const user = await this.userRepo.findOne({
      where: { id: payload.sub },
      select: ["id", "username", "email", "role", "isActive"],
    });

    if (!user || !user.isActive) {
      throw new UnauthorizedException("用户不存在或已被禁用");
    }

    return user; // 附加到 request.user
  }
}
```

---

## Auth Service

```typescript
// src/modules/auth/auth.service.ts
import {
  Injectable,
  UnauthorizedException,
  ConflictException,
  BadRequestException,
} from "@nestjs/common";
import { JwtService } from "@nestjs/jwt";
import { InjectRepository } from "@nestjs/typeorm";
import { Repository, MoreThan } from "typeorm";
import * as bcrypt from "bcrypt";
import { User, UserRole } from "./entities/user.entity";
import { RefreshToken } from "./entities/refresh-token.entity";
import { jwtConstants } from "./constants";

@Injectable()
export class AuthService {
  constructor(
    @InjectRepository(User)
    private userRepo: Repository<User>,
    @InjectRepository(RefreshToken)
    private tokenRepo: Repository<RefreshToken>,
    private jwtService: JwtService
  ) {}

  // 注册
  async register(dto: {
    name: string;
    username: string;
    email: string;
    password: string;
  }) {
    // 检查用户名和邮箱是否已存在
    const existingUser = await this.userRepo.findOne({
      where: [{ username: dto.username }, { email: dto.email }],
    });

    if (existingUser) {
      if (existingUser.username === dto.username) {
        throw new ConflictException("用户名已被注册");
      }
      throw new ConflictException("邮箱已被注册");
    }

    const user = this.userRepo.create({
      name: dto.name,
      username: dto.username,
      email: dto.email,
      password: dto.password, // 通过 setter 触发加密
      role: UserRole.READER,
    });

    await this.userRepo.save(user);

    // 生成 Token
    return this.generateTokens(user);
  }

  // 登录
  async login(dto: { username: string; password: string }) {
    // 查询用户（包含密码）
    const user = await this.userRepo
      .createQueryBuilder("user")
      .where("user.username = :username OR user.email = :username", {
        username: dto.username,
      })
      .addSelect("user.passwordHash")
      .getOne();

    if (!user) {
      throw new UnauthorizedException("用户名或密码错误");
    }

    // 验证密码
    const isPasswordValid = await bcrypt.compare(
      dto.password,
      user.passwordHash
    );
    if (!isPasswordValid) {
      throw new UnauthorizedException("用户名或密码错误");
    }

    if (!user.isActive) {
      throw new UnauthorizedException("账号已被禁用");
    }

    return this.generateTokens(user);
  }

  // 刷新 Token
  async refreshToken(refreshToken: string) {
    // 查找刷新令牌
    const token = await this.tokenRepo.findOne({
      where: { token: refreshToken },
      relations: ["user"],
    });

    if (!token || !token.isValid) {
      throw new UnauthorizedException("无效的刷新令牌");
    }

    // 撤销旧令牌
    token.revoked = true;
    await this.tokenRepo.save(token);

    // 生成新令牌
    return this.generateTokens(token.user);
  }

  // 登出
  async logout(userId: number) {
    // 撤销所有刷新令牌
    await this.tokenRepo.update({ userId, revoked: false }, { revoked: true });
  }

  // 生成访问令牌和刷新令牌
  private async generateTokens(user: User) {
    const payload = {
      sub: user.id,
      username: user.username,
      role: user.role,
    };

    // 访问令牌
    const accessToken = this.jwtService.sign(payload, {
      expiresIn: jwtConstants.accessTokenExpiration,
    });

    // 刷新令牌
    const refreshToken = this.jwtService.sign(payload, {
      expiresIn: jwtConstants.refreshTokenExpiration,
    });

    // 保存刷新令牌到数据库
    const expiresAt = new Date();
    expiresAt.setDate(expiresAt.getDate() + 7); // 7 天后过期

    await this.tokenRepo.save({
      userId: user.id,
      token: refreshToken,
      expiresAt,
    });

    return {
      accessToken,
      refreshToken,
      user: {
        id: user.id,
        name: user.name,
        username: user.username,
        email: user.email,
        role: user.role,
      },
    };
  }
}
```

---

## 角色权限控制

### 自定义装饰器

```typescript
// src/modules/auth/decorators/roles.decorator.ts
import { SetMetadata } from "@nestjs/common";
import { UserRole } from "../entities/user.entity";

export const ROLES_KEY = "roles";
export const Roles = (...roles: UserRole[]) => SetMetadata(ROLES_KEY, roles);

// 装饰器：获取当前用户
// src/modules/auth/decorators/current-user.decorator.ts
import { createParamDecorator, ExecutionContext } from "@nestjs/common";

export const CurrentUser = createParamDecorator(
  (data: string, ctx: ExecutionContext) => {
    const request = ctx.switchToHttp().getRequest();
    const user = request.user;
    return data ? user?.[data] : user;
  }
);
```

### Roles Guard

```typescript
// src/modules/auth/guards/roles.guard.ts
import {
  Injectable,
  CanActivate,
  ExecutionContext,
  ForbiddenException,
} from "@nestjs/common";
import { Reflector } from "@nestjs/core";
import { UserRole } from "../entities/user.entity";
import { ROLES_KEY } from "../decorators/roles.decorator";

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private reflector: Reflector) {}

  canActivate(context: ExecutionContext): boolean {
    // 获取路由所需的角色
    const requiredRoles = this.reflector.getAllAndOverride<UserRole[]>(
      ROLES_KEY,
      [context.getHandler(), context.getClass()]
    );

    // 如果没有设置角色要求，允许访问
    if (!requiredRoles || requiredRoles.length === 0) {
      return true;
    }

    const { user } = context.switchToHttp().getRequest();

    if (!user) {
      throw new ForbiddenException("未认证");
    }

    const hasRole = requiredRoles.some((role) => user.role === role);

    if (!hasRole) {
      throw new ForbiddenException("权限不足");
    }

    return true;
  }
}
```

### 资源所有者 Guard

```typescript
// src/modules/auth/guards/owner.guard.ts
import {
  Injectable,
  CanActivate,
  ExecutionContext,
  ForbiddenException,
} from "@nestjs/common";
import { InjectRepository } from "@nestjs/typeorm";
import { Repository } from "typeorm";
import { Post } from "../../posts/entities/post.entity";

@Injectable()
export class PostOwnerGuard implements CanActivate {
  constructor(
    @InjectRepository(Post)
    private postRepo: Repository<Post>
  ) {}

  async canActivate(context: ExecutionContext): Promise<boolean> {
    const request = context.switchToHttp().getRequest();
    const user = request.user;
    const postId = Number(request.params.id);

    const post = await this.postRepo.findOneBy({ id: postId });

    if (!post) {
      return true; // 让后续逻辑处理 404
    }

    // 管理员可以操作所有文章
    if (user.role === "ADMIN") {
      return true;
    }

    // 只有文章作者才能操作
    if (post.authorId !== user.id) {
      throw new ForbiddenException("只能操作自己的文章");
    }

    return true;
  }
}
```

---

## Auth Controller

```typescript
// src/modules/auth/auth.controller.ts
import {
  Controller,
  Post,
  Body,
  UseGuards,
  HttpCode,
  HttpStatus,
} from "@nestjs/common";
import { AuthGuard } from "@nestjs/passport";
import { AuthService } from "./auth.service";
import { RolesGuard } from "./guards/roles.guard";
import { CurrentUser } from "./decorators/current-user.decorator";
import { User } from "./entities/user.entity";

@Controller("auth")
export class AuthController {
  constructor(private authService: AuthService) {}

  // POST /auth/register - 注册
  @Post("register")
  @HttpCode(HttpStatus.CREATED)
  async register(
    @Body()
    body: {
      name: string;
      username: string;
      email: string;
      password: string;
    }
  ) {
    const result = await this.authService.register(body);
    return {
      success: true,
      data: result,
      message: "注册成功",
    };
  }

  // POST /auth/login - 登录
  @Post("login")
  @HttpCode(HttpStatus.OK)
  async login(@Body() body: { username: string; password: string }) {
    const result = await this.authService.login(body);
    return {
      success: true,
      data: result,
      message: "登录成功",
    };
  }

  // POST /auth/refresh - 刷新 Token
  @Post("refresh")
  @HttpCode(HttpStatus.OK)
  async refreshToken(@Body("refreshToken") refreshToken: string) {
    const result = await this.authService.refreshToken(refreshToken);
    return {
      success: true,
      data: result,
    };
  }

  // POST /auth/logout - 登出
  @Post("logout")
  @UseGuards(AuthGuard("jwt"))
  @HttpCode(HttpStatus.OK)
  async logout(@CurrentUser() user: User) {
    await this.authService.logout(user.id);
    return {
      success: true,
      message: "已登出",
    };
  }
}
```

---

## 在 Posts 模块中使用认证

```typescript
// 更新 PostsController，添加认证和权限控制

@Controller("posts")
@UseGuards(AuthGuard("jwt"), RolesGuard) // 全局 JWT 认证
export class PostsController {
  constructor(private readonly postsService: PostsService) {}

  // 创建文章 - 需要 AUTHOR 及以上角色
  @Post()
  @Roles(UserRole.ADMIN, UserRole.EDITOR, UserRole.AUTHOR)
  async create(
    @Body() createPostDto: CreatePostDto,
    @CurrentUser() user: User
  ) {
    // 使用当前用户的 ID 作为作者
    createPostDto.authorId = user.id;
    const post = await this.postsService.create(createPostDto);
    return { success: true, data: post };
  }

  // 查询文章列表 - 不需要认证
  @Get()
  @Public() // 标记为公开接口
  async findAll(@Query() pagination: PaginationDto) {
    const result = await this.postsService.findAll(pagination);
    return { success: true, data: result };
  }

  // 更新文章 - 只有作者或管理员可以操作
  @Put(":id")
  @UseGuards(PostOwnerGuard) // 资源级权限检查
  async update(
    @Param("id", ParseIntPipe) id: number,
    @Body() updatePostDto: UpdatePostDto
  ) {
    const post = await this.postsService.update(id, updatePostDto);
    return { success: true, data: post };
  }

  // 删除文章 - 只有管理员可以操作
  @Delete(":id")
  @Roles(UserRole.ADMIN)
  async remove(@Param("id", ParseIntPipe) id: number) {
    await this.postsService.remove(id);
    return { success: true, message: "删除成功" };
  }
}
```

### Public 装饰器

```typescript
// src/modules/auth/decorators/public.decorator.ts
import { SetMetadata } from "@nestjs/common";

export const IS_PUBLIC_KEY = "isPublic";
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
```

### 更新 JwtGuard 以支持 Public

```typescript
// src/modules/auth/guards/jwt-auth.guard.ts
import { Injectable, ExecutionContext } from "@nestjs/common";
import { AuthGuard } from "@nestjs/passport";
import { Reflector } from "@nestjs/core";
import { IS_PUBLIC_KEY } from "../decorators/public.decorator";

@Injectable()
export class JwtAuthGuard extends AuthGuard("jwt") {
  constructor(private reflector: Reflector) {
    super();
  }

  canActivate(context: ExecutionContext) {
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);

    if (isPublic) {
      return true;
    }

    return super.canActivate(context);
  }
}
```

---

## Auth 模块注册

```typescript
// src/modules/auth/auth.module.ts
import { Module } from "@nestjs/common";
import { JwtModule } from "@nestjs/jwt";
import { PassportModule } from "@nestjs/passport";
import { TypeOrmModule } from "@nestjs/typeorm";
import { AuthService } from "./auth.service";
import { AuthController } from "./auth.controller";
import { JwtStrategy } from "./strategies/jwt.strategy";
import { RolesGuard } from "./guards/roles.guard";
import { User } from "./entities/user.entity";
import { RefreshToken } from "./entities/refresh-token.entity";
import { jwtConstants } from "./constants";

@Module({
  imports: [
    TypeOrmModule.forFeature([User, RefreshToken]),
    PassportModule.register({ defaultStrategy: "jwt" }),
    JwtModule.register({
      global: true,
      secret: jwtConstants.secret,
      signOptions: {
        expiresIn: jwtConstants.accessTokenExpiration,
      },
    }),
  ],
  controllers: [AuthController],
  providers: [AuthService, JwtStrategy, RolesGuard],
  exports: [AuthService, JwtModule],
})
export class AuthModule {}
```

---

## 总结

### 核心要点

1. **实体设计**
   - User：用户信息 + 密码加密
   - RefreshToken：令牌管理 + 过期检查

2. **认证流程**
   - 注册：验证唯一性 → 创建用户 → 生成 Token
   - 登录：查找用户 → 验证密码 → 生成 Token
   - 刷新：验证 RefreshToken → 撤销旧令牌 → 生成新令牌

3. **JWT 策略**
   - PassportStrategy 自动验证 Token
   - validate 方法返回的用户附加到 request.user

4. **权限控制**
   - @Roles 装饰器：角色级别
   - PostOwnerGuard：资源级别
   - @Public 装饰器：公开接口

### 下一步

在下一篇文章中，我们将学习：

- **性能优化与最佳实践**
- N+1 查询问题及解决方案
- 连接池调优、批量操作优化
- 查询缓存和索引策略

敬请期待！

---

## 参考资源

- [NestJS Authentication 文档](https://docs.nestjs.com/security/authentication)
- [NestJS Authorization 文档](https://docs.nestjs.com/security/authorization)
- [Passport.js 文档](http://www.passportjs.org/)

---

**上一篇**：[08. NestJS 集成实战：博客 API](./08-nestjs-blog-api.mdx)
**下一篇**：[10. 性能优化与最佳实践](./10-performance.mdx)
