---
title: "13. Docker 监控与日志：容器可观测性"
date: "2025-03-23"
summary: "构建完整的 Docker 监控体系，掌握容器日志收集、资源监控和性能分析。"
tags: ["Docker", "监控", "日志", "Prometheus"]
category: "docker"
cover: ""
draft: false
series: "Docker 从入门到实战"
seriesOrder: 13
---

## 监控架构

### 完整监控栈

```yaml
version: "3.8"

services:
  # Prometheus
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"

  # Grafana
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana-data:/var/lib/grafana

  # cAdvisor
  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    ports:
      - "8080:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro

volumes:
  prometheus-data:
  grafana-data:
```

## 容器监控

### docker stats

```bash
# 实时监控
docker stats

# 输出格式化
docker stats --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}"

# 只显示特定容器
docker stats web api
```

### Prometheus 配置

```yaml
# prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "cadvisor"
    static_configs:
      - targets: ["cadvisor:8080"]
```

## 日志管理

### 日志驱动配置

```yaml
version: "3.8"

services:
  app:
    image: myapp:latest
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

### 集中日志收集

```yaml
services:
  # ELK Stack
  elasticsearch:
    image: elasticsearch:8.0.0
    environment:
      - discovery.type=single-node
    volumes:
      - es-data:/usr/share/elasticsearch/data

  logstash:
    image: logstash:8.0.0
    volumes:
      - ./logstash.conf:/usr/share/logstash/pipeline/logstash.conf

  kibana:
    image: kibana:8.0.0
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch
```

## 健康检查

### 应用健康检查

```dockerfile
FROM node:18-alpine

HEALTHCHECK --interval=30s --timeout=3s \
  CMD node healthcheck.js || exit 1

CMD ["node", "server.js"]
```

## 告警配置

### AlertManager

```yaml
alertmanager:
  image: prom/alertmanager:latest
  ports:
    - "9093:9093"
  volumes:
    - ./alertmanager.yml:/etc/alertmanager/alertmanager.yml
```

## 最佳实践

1. **日志轮转**：防止磁盘占满
2. **监控指标**：CPU、内存、网络、磁盘
3. **告警规则**：及时发现问题
4. **日志聚合**：集中管理
5. **可视化**：Grafana 仪表盘

## 总结

监控和日志是生产环境必备：

- **监控**：Prometheus + Grafana + cAdvisor
- **日志**：ELK Stack 或 Loki
- **告警**：AlertManager

下一步：Docker 多阶段构建深入
