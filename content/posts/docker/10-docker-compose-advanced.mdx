---
title: "10. Docker Compose 进阶实战：复杂应用编排"
date: "2025-03-23"
summary: "掌握 Docker Compose 的高级用法，包括环境配置、健康检查、服务扩展和生产部署。"
tags: ["Docker", "Docker Compose", "编排"]
category: "docker"
cover: ""
draft: false
series: "Docker 从入门到实战"
seriesOrder: 10
---

## 环境变量管理

### 多环境配置

```yaml
# docker-compose.yml
version: "3.8"

services:
  app:
    image: myapp:${APP_VERSION:-latest}
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - DATABASE_URL=postgres://${DB_USER:-user}:${DB_PASS:-pass}@db:5432/${DB_NAME:-myapp}
      - REDIS_URL=redis://redis:6379
    env_file:
      - .env
      - .env.${NODE_ENV}
```

```bash
# .env.production
NODE_ENV=production
APP_VERSION=1.0.0
DB_USER=produser
DB_PASS=strong_password_here
DB_NAME=production_db

# 启动生产环境
docker compose --env-file .env.production up -d
```

### secrets 管理

```yaml
version: "3.8"

services:
  db:
    image: postgres:15
    secrets:
      - db_password
    environment:
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password

secrets:
  db_password:
    file: ./secrets/db_password.txt
```

```bash
# 创建 secret
echo "my_secure_password" > secrets/db_password.txt
chmod 600 secrets/db_password.txt
```

## 健康检查

### 配置健康检查

```yaml
services:
  web:
    image: nginx:alpine
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://localhost/health",
        ]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s

  backend:
    image: myapp:latest
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 20s
      timeout: 5s
      retries: 5
```

### 依赖健康检查

```yaml
services:
  web:
    image: nginx
    depends_on:
      backend:
        condition: service_healthy

  backend:
    image: myapp
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
```

## 服务扩展

### 水平扩展

```yaml
version: '3.8'

services:
  web:
    image: nginx
    deploy:
      mode: replicated
      replicas: 3

# 手动扩展
docker compose up -d --scale web=5
```

### 资源限制

```yaml
services:
  app:
    image: myapp
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
```

## 生产部署配置

### 完整生产配置

```yaml
version: "3.8"

services:
  web:
    image: myapp:${VERSION:-latest}
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - frontend
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        restart_policy:
          condition: on-failure
          delay: 5s
          max_attempts: 3

  api:
    image: myapp-api:${VERSION:-latest}
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgres://user:pass@db:5432/myapp
    depends_on:
      db:
        condition: service_healthy
    networks:
      - frontend
      - backend

  db:
    image: postgres:15-alpine
    restart: unless-stopped
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: myapp
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - backend

volumes:
  postgres-data:

networks:
  frontend:
  backend:
```

## 常用命令

### 服务管理

```bash
# 查看服务状态
docker compose ps

# 查看服务日志
docker compose logs -f web

# 重启服务
docker compose restart web

# 重新构建并启动
docker compose up -d --build

# 停止并删除
docker compose down

# 删除所有资源（包括卷）
docker compose down -v
```

### 批量操作

```bash
# 停止所有服务
docker compose stop

# 启动所有服务
docker compose start

# 查看资源使用
docker compose top

# 查看实时日志
docker compose logs --tail=100 -f
```

## 最佳实践

1. **使用 .env 文件管理配置**
2. **设置健康检查确保服务可用**
3. **限制资源防止耗尽宿主机**
4. **使用命名卷持久化数据**
5. **配置日志轮转防止磁盘占满**

## 总结

Docker Compose 进阶要点：

- **环境变量**：`.env` 文件和多环境配置
- **健康检查**：确保服务正常
- **服务扩展**：水平扩展和资源限制
- **生产部署**：完整的配置示例

下一步：Docker 镜像优化
