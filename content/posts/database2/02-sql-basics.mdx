---
title: "02. SQL 语言基础：掌握数据库查询的核心技能"
date: "2026-03-19"
summary: "系统学习 SQL 语言的核心语法, 数据操作和最佳实践，包含丰富的实战示例"
tags: ["SQL", "PostgreSQL", "数据库", "查询"]
category: "数据库"
cover: ""
draft: false
series: "数据库渐进式学习路线"
seriesOrder: 2
---

上一篇文章我们了解了数据库的基本概念，知道关系型数据库用表格存储数据。但怎么和数据库对话呢？这就需要 SQL 语言。

SQL（Structured Query Language，结构化查询语言）是与关系型数据库交互的标准语言。无论你使用 PostgreSQL, MySQL, SQLite 还是其他关系型数据库，SQL 都是必备技能。

SQL 有个特点很有意思：它是声明式语言，不是命令式。这是什么意思呢？在编程语言里，你告诉计算机"怎么做"：先遍历数组，再判断条件，然后执行操作。但在 SQL 里，你只需要告诉数据库"要什么"：给我所有已支付的订单。数据库自己决定如何高效地获取这些数据。

这种声明式的思维方式需要一些适应。写 SQL 时，你需要关注的是数据的集合，而不是单条记录的处理流程。

SQL 按功能可以分为五大类。DDL（Data Definition Language）数据定义语言用于定义数据库结构，包括 CREATE, ALTER, DROP, TRUNCATE。DML（Data Manipulation Language）数据操作语言用于操作数据，包括 INSERT, UPDATE, DELETE, MERGE。DQL（Data Query Language）数据查询语言用于查询数据，主要是 SELECT。DCL（Data Control Language）数据控制语言用于权限控制，包括 GRANT, REVOKE, DENY。TCL（Transaction Control Language）事务控制语言用于事务管理，包括 COMMIT, ROLLBACK, SAVEPOINT。

让我们通过一个电商系统的示例，系统学习 SQL 的各种语法。这个案例将贯穿整个系列，帮助我们理解如何在实际项目中使用数据库。

首先创建数据库和切换到该数据库：

```sql
CREATE DATABASE ecommerce;
\c ecommerce;
```

现在开始创建表。我们用 DDL 定义用户表, 商品分类表, 商品表, 订单表和订单项表：

```sql
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  full_name VARCHAR(100),
  phone VARCHAR(20),
  is_verified BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE categories (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  slug VARCHAR(100) UNIQUE NOT NULL,
  description TEXT,
  parent_id INTEGER REFERENCES categories(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE products (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  slug VARCHAR(255) UNIQUE NOT NULL,
  description TEXT,
  price NUMERIC(10, 2) NOT NULL,
  stock INTEGER DEFAULT 0,
  category_id INTEGER REFERENCES categories(id) ON DELETE SET NULL,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE orders (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  order_number VARCHAR(50) UNIQUE NOT NULL,
  status VARCHAR(20) DEFAULT 'pending',
  subtotal NUMERIC(10, 2) NOT NULL,
  tax NUMERIC(10, 2) DEFAULT 0,
  discount NUMERIC(10, 2) DEFAULT 0,
  total NUMERIC(10, 2) NOT NULL,
  notes TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE order_items (
  id SERIAL PRIMARY KEY,
  order_id INTEGER NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
  product_id INTEGER NOT NULL REFERENCES products(id) ON DELETE RESTRICT,
  quantity INTEGER NOT NULL CHECK (quantity > 0),
  unit_price NUMERIC(10, 2) NOT NULL,
  subtotal NUMERIC(10, 2) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

这里有很多重要的约束和设置。SERIAL 是 PostgreSQL 的自增整数类型。PRIMARY KEY 定义主键，唯一标识每一行。UNIQUE 约束保证值不能重复。NOT NULL 约束保证值不能为空。DEFAULT 设置默认值。REFERENCES 定义外键约束，建立表之间的关系。ON DELETE CASCADE 表示删除父记录时子记录也删除，ON DELETE SET NULL 表示删除父记录时子记录的外键设为 NULL，ON DELETE RESTRICT 表示有子记录时禁止删除父记录。CHECK 约束用于验证数据，如 quantity > 0。

创建表后可能需要修改表结构。ALTER TABLE 可以添加列, 修改列类型, 重命名列, 删除列, 添加约束, 添加索引：

```sql
ALTER TABLE users ADD COLUMN avatar_url TEXT;
ALTER TABLE users ADD COLUMN last_login_at TIMESTAMP;
ALTER TABLE products ALTER COLUMN price TYPE NUMERIC(12, 2);
ALTER TABLE users RENAME COLUMN full_name TO name;
ALTER TABLE users DROP COLUMN phone;
ALTER TABLE users ADD CONSTRAINT check_email_format
  CHECK (email ~* '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$');
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_products_category ON products(category_id);
CREATE INDEX idx_orders_user_status ON orders(user_id, status);
```

删除表时可以使用 DROP TABLE 或 TRUNCATE。DROP TABLE 删除表结构和数据，TRUNCATE 只删除数据保留表结构：

```sql
DROP TABLE IF EXISTS order_items;
TRUNCATE TABLE products RESTART IDENTITY CASCADE;
```

表结构定义好后，使用 DML 操作数据。INSERT 插入数据：

```sql
INSERT INTO users (email, password_hash, name)
VALUES ('zhangsan@example.com', 'hashed_password_123', '张三');

INSERT INTO users (email, password_hash, name) VALUES
  ('lisi@example.com', 'hashed_password_456', '李四'),
  ('wangwu@example.com', 'hashed_password_789', '王五');

INSERT INTO products (name, slug, price, stock)
VALUES ('iPhone 15', 'iphone-15', 7999.00, 100)
RETURNING *;
```

UPDATE 更新数据：

```sql
UPDATE users
SET name = '张三丰', is_verified = TRUE
WHERE id = 1;

UPDATE products
SET stock = stock - 1
WHERE id = 1 AND stock > 0;
```

DELETE 删除数据：

```sql
DELETE FROM users WHERE id = 1;
DELETE FROM orders
WHERE status = 'cancelled' AND created_at < '2024-01-01';
```

最常用的是 DQL 查询数据。基础查询可以这样写：

```sql
SELECT * FROM users;
SELECT id, email, name FROM users;
SELECT
  id AS user_id,
  email AS user_email,
  name AS user_name
FROM users;
SELECT DISTINCT status FROM orders;
SELECT * FROM orders
ORDER BY created_at DESC
LIMIT 10 OFFSET 20;
```

WHERE 条件过滤支持各种运算符：

```sql
SELECT * FROM products WHERE price > 1000;
SELECT * FROM users
WHERE is_verified = TRUE AND created_at >= '2024-01-01';
SELECT * FROM orders
WHERE created_at BETWEEN '2024-01-01' AND '2024-12-31';
SELECT * FROM orders
WHERE status IN ('paid', 'shipped', 'delivered');
SELECT * FROM users WHERE phone IS NULL;
SELECT * FROM products
WHERE name LIKE '%iPhone%';
```

ORDER BY 用于排序：

```sql
SELECT * FROM products ORDER BY price ASC;
SELECT * FROM products ORDER BY price DESC;
SELECT * FROM orders
ORDER BY status ASC, created_at DESC;
SELECT * FROM users
ORDER BY last_login_at DESC NULLS LAST;
```

聚合函数用于统计分析：

```sql
SELECT COUNT(*) FROM users;
SELECT SUM(total) FROM orders WHERE status = 'paid';
SELECT AVG(price) FROM products;
SELECT MIN(price), MAX(price) FROM products;
SELECT
  COUNT(*) as total_orders,
  SUM(total) as total_revenue,
  AVG(total) as avg_order_value
FROM orders
WHERE status = 'paid';
```

GROUP BY 分组统计：

```sql
SELECT
  status,
  COUNT(*) as order_count,
  SUM(total) as total_revenue
FROM orders
GROUP BY status
ORDER BY total_revenue DESC;

SELECT
  user_id,
  COUNT(*) as order_count,
  SUM(total) as total_spent
FROM orders
WHERE status = 'paid'
GROUP BY user_id
HAVING SUM(total) > 10000
ORDER BY total_spent DESC;
```

WHERE 和 HAVING 的区别很重要。WHERE 在分组前过滤，不能使用聚合函数。HAVING 在分组后过滤，可以使用聚合函数。

JOIN 连接查询用于关联多个表的数据：

```sql
SELECT
  o.id,
  o.order_number,
  o.total,
  u.email,
  u.name
FROM orders o
INNER JOIN users u ON o.user_id = u.id;

SELECT
  u.id,
  u.email,
  COUNT(o.id) as order_count
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
GROUP BY u.id;

SELECT
  o.order_number,
  p.name as product_name,
  oi.quantity,
  oi.unit_price
FROM orders o
INNER JOIN order_items oi ON o.id = oi.order_id
INNER JOIN products p ON oi.product_id = p.id
WHERE o.status = 'paid';
```

INNER JOIN 只匹配两边都有的数据，LEFT JOIN 保留左表所有行，RIGHT JOIN 保留右表所有行，FULL JOIN 保留两边所有行。

子查询可以在 WHERE, FROM, SELECT 中使用：

```sql
SELECT * FROM products
WHERE price > (SELECT AVG(price) FROM products);

SELECT * FROM products
WHERE category_id IN (SELECT id FROM categories WHERE name LIKE '电子%');

SELECT
  user_stats.user_id,
  user_stats.order_count,
  u.email
FROM (
  SELECT
    user_id,
    COUNT(*) as order_count
  FROM orders
  GROUP BY user_id
) user_stats
JOIN users u ON user_stats.user_id = u.id;
```

理解 SQL 的执行顺序对编写复杂查询很重要。语法顺序是我们写的顺序：SELECT DISTINCT column FROM table JOIN condition WHERE condition GROUP BY column HAVING condition ORDER BY column LIMIT 10。但实际执行顺序是：先 FROM 确定数据来源，再 JOIN 连接表，然后 WHERE 过滤行，GROUP BY 分组，HAVING 过滤分组，SELECT 选择列，DISTINCT 去重，ORDER BY 排序，最后 LIMIT 限制结果。

看个具体例子：

```sql
SELECT
  u.name,
  COUNT(o.id) as order_count
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
WHERE u.is_verified = TRUE
GROUP BY u.id, u.name
HAVING COUNT(o.id) > 0
ORDER BY order_count DESC
LIMIT 10;
```

这条查询的执行顺序是：先 FROM users 获取 users 表，然后 LEFT JOIN orders 连接 orders 表，WHERE u.is_verified = TRUE 过滤未验证用户，GROUP BY u.id, u.name 按用户分组，HAVING COUNT(o.id) > 0 过滤无订单用户，SELECT u.name, COUNT(o.id) 选择显示列，ORDER BY order_count DESC 按订单数降序，最后 LIMIT 10 只取前 10 名。

SQL 编写有一些好的实践。使用大写写 SQL 关键字，每行一个列名便于阅读，使用表别名简化代码，明确列出列名避免使用 SELECT \*，添加注释解释复杂逻辑。

命名规范也很重要。表名使用小写加下划线, 复数形式，列名使用小写加下划线，主键可以用表名加 _id 或 id，外键用关联表名加 \_id，索引用 idx_ 加表名加 _ 加列名，布尔列用 is_ 前缀，时间列用 \_at 后缀。

我们来看几个电商系统的实际查询例子。用户订单统计查询：

```sql
SELECT
  u.id,
  u.email,
  u.name,
  COUNT(o.id) as total_orders,
  COALESCE(SUM(o.total), 0) as total_spent,
  COALESCE(AVG(o.total), 0) as avg_order_value
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
WHERE u.is_verified = TRUE
GROUP BY u.id, u.email, u.name
ORDER BY total_spent DESC
LIMIT 20;
```

热销商品排行榜：

```sql
SELECT
  p.id,
  p.name,
  p.price,
  SUM(oi.quantity) as total_sold,
  SUM(oi.subtotal) as total_revenue,
  COUNT(DISTINCT oi.order_id) as order_count
FROM products p
INNER JOIN order_items oi ON p.id = oi.product_id
INNER JOIN orders o ON oi.order_id = o.id
WHERE o.status IN ('paid', 'shipped', 'delivered')
  AND o.created_at >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY p.id, p.name, p.price
ORDER BY total_sold DESC
LIMIT 10;
```

每日订单趋势：

```sql
SELECT
  DATE(created_at) as order_date,
  COUNT(*) as total_orders,
  COUNT(DISTINCT user_id) as unique_customers,
  SUM(total) as total_revenue,
  AVG(total) as avg_order_value
FROM orders
WHERE created_at >= CURRENT_DATE - INTERVAL '30 days'
  AND status = 'paid'
GROUP BY DATE(created_at)
ORDER BY order_date DESC;
```

库存预警查询：

```sql
SELECT
  p.id,
  p.name,
  p.stock,
  c.name as category_name,
  (
    SELECT SUM(quantity)
    FROM order_items oi
    JOIN orders o ON oi.order_id = o.id
    WHERE oi.product_id = p.id
      AND o.status IN ('pending', 'paid')
  ) as pending_orders
FROM products p
LEFT JOIN categories c ON p.category_id = c.id
WHERE p.stock < 10
  AND p.is_active = TRUE
ORDER BY p.stock ASC;
```

掌握 SQL 基础后，下一步我们将学习 PostgreSQL 快速入门，了解如何搭建环境, 使用 psql 工具，以及 PostgreSQL 的特有功能如 JSONB, 数组, 枚举类型等，为后续深入学习打下基础。
