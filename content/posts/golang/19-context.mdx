---
title: "19. Context 使用"
date: "2026-03-23"
summary: "深入理解 Go 语言的 context 包，学习如何管理请求超时、取消和传递请求范围的值。"
tags: ["Go", "Golang", "context", "超时控制", "并发"]
category: "golang"
cover: ""
draft: false
series: "Go 语言渐进式学习"
seriesOrder: 19
---

## 前言

context 包是 Go 1.7 引入的标准库，用于在 goroutine 之间传递截止日期、取消信号和请求范围的值。

---

## Context 基础

### Context 接口

```go
type Context interface {
    Deadline() (deadline time.Time, ok bool)
    Done() <-chan struct{}
    Err() error
    Value(key interface{}) interface{}
}
```

### 创建 Context

```go
package main

import (
    "context"
    "fmt"
    "time"
)

func main() {
    // 创建根 context
    ctx := context.Background()

    // 创建可取消的 context
    ctx, cancel := context.WithCancel(ctx)
    defer cancel()

    go func() {
        <-ctx.Done()
        fmt.Println("goroutine 被取消")
    }()

    time.Sleep(100 * time.Millisecond)
}
```

---

## 超时控制

### WithTimeout

```go
package main

import (
    "context"
    "fmt"
    "time"
)

func slowOperation(ctx context.Context) error {
    select {
    case <-time.After(2 * time.Second):
        fmt.Println("操作完成")
        return nil
    case <-ctx.Done():
        return ctx.Err()
    }
}

func main() {
    ctx, cancel := context.WithTimeout(context.Background(), 1*time.Second)
    defer cancel()

    err := slowOperation(ctx)
    if err != nil {
        fmt.Println("错误:", err)
    }
}
```

### WithDeadline

```go
package main

import (
    "context"
    "fmt"
    "time"
)

func main() {
    deadline := time.Now().Add(1 * time.Second)
    ctx, cancel := context.WithDeadline(context.Background(), deadline)
    defer cancel()

    select {
    case <-time.After(2 * time.Second):
        fmt.Println("操作完成")
    case <-ctx.Done():
        fmt.Println("超时:", ctx.Err())
    }
}
```

---

## 传递值

### WithValue

```go
package main

import (
    "context"
    "fmt"
)

type key string

const userKey key = "user"

type User struct {
    Name string
}

func main() {
    user := User{Name: "张三"}

    // 存储值
    ctx := context.WithValue(context.Background(), userKey, user)

    // 获取值
    if u, ok := ctx.Value(userKey).(User); ok {
        fmt.Println("用户:", u.Name)
    }
}
```

---

## 最佳实践

### 传递 Context

```go
// ✅ 推荐：Context 作为第一个参数
func Process(ctx context.Context, data string) error {
    // ...
}

// ❌ 避免：不传递 Context
func Process(data string) error {
    // ...
}
```

### 不要存储 Context

```go
// ❌ 避免
type Server struct {
    ctx context.Context
}

// ✅ 推荐：每次调用传递
func (s *Server) Process(ctx context.Context) error {
    // ...
}
```

---

## 总结

本文学习了：

- Context 接口
- 超时控制
- 传递值
- 最佳实践

下一步：[20 实战项目：RESTful API](./20-restful-api.mdx)
