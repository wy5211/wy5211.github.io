---
title: "01. MyBatis 快速入门：为什么国内企业都选择它"
date: "2026-03-23"
summary: "了解 MyBatis 的核心优势，掌握环境搭建，完成第一个 MyBatis 程序。"
tags: ["MyBatis", "ORM", "Java", "入门"]
category: "mybatis"
cover: ""
draft: false
series: "MyBatis 渐进式学习路线"
seriesOrder: 1
---

## 前言

在国内 Java 开发中，MyBatis 是最常用的持久层框架。阿里巴巴、美团、京东等大厂都在使用它。

**为什么 MyBatis 如此受欢迎？**

- 🔥 **SQL 灵活**：完全掌控 SQL，适合复杂业务
- 🚀 **性能优秀**：优秀的性能表现
- 📚 **学习简单**：比 Hibernate 更容易上手
- 💼 **就业必备**：国内企业招聘必备技能

本文将带你从零开始掌握 MyBatis。

---

## 什么是 MyBatis？

### MyBatis 简介

MyBatis 是一款优秀的**持久层框架**，它支持定制化 SQL、存储过程以及高级映射。

```
Java 应用 → MyBatis → SQL → MySQL 数据库
   ↑                    ↓
   └────────────────────┘
     结果映射（Java 对象）
```

### 核心特性

**1. SQL 与代码分离**

```xml
<!-- SQL 写在 XML 文件中 -->
<select id="selectUser" resultType="User">
    SELECT * FROM users WHERE id = #{id}
</select>
```

**2. 动态 SQL**

```xml
<!-- 根据条件动态生成 SQL -->
<select id="findUsers" resultType="User">
    SELECT * FROM users
    <where>
        <if test="username != null">
            AND username = #{username}
        </if>
        <if test="email != null">
            AND email = #{email}
        </if>
    </where>
</select>
```

**3. 结果映射**

```java
// 自动将数据库结果映射到 Java 对象
User user = userMapper.selectById(1L);
```

---

## MyBatis vs JDBC vs Hibernate

### 繁琐的 JDBC 代码

```java
// ❌ JDBC 需要大量代码
public User getUserById(Long id) {
    User user = null;
    Connection conn = null;
    PreparedStatement pstmt = null;
    ResultSet rs = null;

    try {
        conn = DriverManager.getConnection(
            "jdbc:mysql://localhost:3306/mydb", "root", "password");
        String sql = "SELECT * FROM users WHERE id = ?";
        pstmt = conn.prepareStatement(sql);
        pstmt.setLong(1, id);
        rs = pstmt.executeQuery();

        if (rs.next()) {
            user = new User();
            user.setId(rs.getLong("id"));
            user.setUsername(rs.getString("username"));
            // ... 更多映射代码
        }
    } finally {
        // 关闭资源...
    }
    return user;
}
```

### 简洁的 MyBatis 代码

```java
// ✅ MyBatis 只需一行代码
@Autowired
private UserMapper userMapper;

public User getUserById(Long id) {
    return userMapper.selectById(id);
}
```

### MyBatis vs Hibernate 对比

| 特性         | MyBatis        | Hibernate (JPA) |
| ------------ | -------------- | --------------- |
| **SQL 控制** | 完全掌控       | 自动生成        |
| **学习曲线** | 平缓           | 陡峭            |
| **复杂查询** | 灵活           | 困难            |
| **性能优化** | 容易           | 困难            |
| **国内使用** | 80%+           | 较少            |
| **适用场景** | 复杂业务、报表 | 简单 CRUD       |

**选择建议**：

- 国内企业、复杂业务 → **MyBatis**
- 简单 CRUD、国外项目 → Hibernate

---

## 环境搭建

### 1. 创建 Maven 项目

```bash
# 创建 Maven 项目
mvn archetype:generate \
  -DgroupId=com.example \
  -DartifactId=mybatis-demo \
  -DarchetypeArtifactId=maven-archetype-quickstart \
  -DinteractiveMode=false

cd mybatis-demo
```

### 2. 添加依赖

```xml
<!-- pom.xml -->
<dependencies>
    <!-- MyBatis 核心依赖 -->
    <dependency>
        <groupId>org.mybatis</groupId>
        <artifactId>mybatis</artifactId>
        <version>3.5.13</version>
    </dependency>

    <!-- MySQL 驱动 -->
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <version>8.0.33</version>
    </dependency>

    <!-- Lombok（简化实体类） -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>1.18.28</version>
        <scope>provided</scope>
    </dependency>

    <!-- JUnit 5（测试） -->
    <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>5.9.3</version>
        <scope>test</scope>
    </dependency>
</dependencies>
```

### 3. 创建数据库

```sql
-- 创建数据库
CREATE DATABASE mybatis_demo;

-- 使用数据库
USE mybatis_demo;

-- 创建用户表
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL,
    age INT,
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 插入测试数据
INSERT INTO users (username, email, age) VALUES
('张三', 'zhangsan@example.com', 25),
('李四', 'lisi@example.com', 30),
('王五', 'wangwu@example.com', 28);
```

---

## MyBatis 核心配置

### mybatis-config.xml

```xml
<!-- src/main/resources/mybatis-config.xml -->
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 环境配置 -->
    <environments default="development">
        <environment id="development">
            <!-- 事务管理器 -->
            <transactionManager type="JDBC"/>

            <!-- 数据源 -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis_demo"/>
                <property name="username" value="root"/>
                <property name="password" value="root123"/>
            </dataSource>
        </environment>
    </environments>

    <!-- Mapper 文件位置 -->
    <mappers>
        <mapper resource="mapper/UserMapper.xml"/>
    </mappers>
</configuration>
```

---

## 第一个 MyBatis 程序

### 1. 创建实体类

```java
package com.example.entity;

import lombok.Data;

import java.time.LocalDateTime;

@Data
public class User {
    private Long id;
    private String username;
    private String email;
    private Integer age;
    private LocalDateTime createTime;
}
```

### 2. 创建 Mapper 接口

```java
package com.example.mapper;

import com.example.entity.User;

public interface UserMapper {
    // 根据 ID 查询用户
    User selectById(Long id);

    // 查询所有用户
    List<User> selectAll();

    // 插入用户
    int insert(User user);

    // 更新用户
    int update(User user);

    // 删除用户
    int deleteById(Long id);
}
```

### 3. 创建 Mapper XML

```xml
<!-- src/main/resources/mapper/UserMapper.xml -->
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.example.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="age" property="age"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 根据 ID 查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM users ORDER BY id
    </select>

    <!-- 插入用户 -->
    <insert id="insert">
        INSERT INTO users (username, email, age)
        VALUES (#{username}, #{email}, #{age})
    </insert>

    <!-- 更新用户 -->
    <update id="update">
        UPDATE users
        SET username = #{username},
            email = #{email},
            age = #{age}
        WHERE id = #{id}
    </update>

    <!-- 删除用户 -->
    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>
</mapper>
```

### 4. 使用 MyBatis

```java
package com.example;

import com.example.entity.User;
import com.example.mapper.UserMapper;
import org.apache.ibatis.io.Resources;
import org.apache.ibatis.session.SqlSession;
import org.apache.ibatis.session.SqlSessionFactory;
import org.apache.ibatis.session.SqlSessionFactoryBuilder;

import java.io.InputStream;
import java.util.List;

public class MyBatisDemo {
    public static void main(String[] args) throws Exception {
        // 1. 读取配置文件
        InputStream inputStream = Resources.getResourceAsStream("mybatis-config.xml");

        // 2. 创建 SqlSessionFactory
        SqlSessionFactory factory = new SqlSessionFactoryBuilder().build(inputStream);

        // 3. 获取 SqlSession
        try (SqlSession session = factory.openSession()) {
            // 4. 获取 Mapper
            UserMapper mapper = session.getMapper(UserMapper.class);

            // 5. 查询单个用户
            User user = mapper.selectById(1L);
            System.out.println("用户信息：" + user);

            // 6. 查询所有用户
            List<User> users = mapper.selectAll();
            System.out.println("所有用户：" + users);

            // 7. 插入用户
            User newUser = new User();
            newUser.setUsername("赵六");
            newUser.setEmail("zhaoliu@example.com");
            newUser.setAge(26);

            int count = mapper.insert(newUser);
            session.commit(); // 提交事务
            System.out.println("插入成功，影响行数：" + count);
        }
    }
}
```

---

## 核心概念

### SqlSessionFactory

```java
// SqlSessionFactory 是工厂类，负责创建 SqlSession
// 它是线程安全的，全局只需要一个
SqlSessionFactory factory = new SqlSessionFactoryBuilder()
    .build(inputStream);
```

### SqlSession

```java
// SqlSession 相当于 JDBC 的 Connection
// 它不是线程安全的，每次使用都要创建新的
try (SqlSession session = factory.openSession()) {
    // 执行数据库操作
    UserMapper mapper = session.getMapper(UserMapper.class);
    // ...
    session.commit(); // 提交事务
}
```

### Mapper 接口

```java
// Mapper 接口定义了数据库操作方法
// MyBatis 会自动生成接口的实现类
public interface UserMapper {
    User selectById(Long id);
}
```

---

## 最佳实践

### 1. 使用工具类管理 SqlSessionFactory

```java
public class MyBatisUtil {
    private static SqlSessionFactory factory;

    static {
        try {
            InputStream inputStream = Resources.getResourceAsStream("mybatis-config.xml");
            factory = new SqlSessionFactoryBuilder().build(inputStream);
        } catch (IOException e) {
            throw new RuntimeException("初始化 MyBatis 失败", e);
        }
    }

    public static SqlSession getSession() {
        return factory.openSession();
    }
}
```

### 2. 命名规范

```java
// ✅ 推荐
UserMapper.java / UserMapper.xml

// ❌ 避免
userMapper.java / userMapper.xml
```

### 3. 资源文件位置

```
src/main/resources/
├── mybatis-config.xml      # MyBatis 配置
└── mapper/
    └── UserMapper.xml      # Mapper XML
```

---

## 常见问题

### Q1: #{id} 和 ${id} 的区别？

**A**:

- `#{id}`：预编译，安全，防止 SQL 注入
- `${id}`：字符串替换，不安全，避免使用

```xml
<!-- ✅ 推荐 -->
SELECT * FROM users WHERE id = #{id}

<!-- ❌ 避免（有 SQL 注入风险） -->
SELECT * FROM users WHERE id = ${id}
```

### Q2: SqlSession 需要关闭吗？

**A**: 是的，使用完后必须关闭：

```java
// 使用 try-with-resources 自动关闭
try (SqlSession session = factory.openSession()) {
    // ...
}  // 自动关闭
```

### Q3: resultMap 和 resultType 的区别？

**A**:

- `resultType`：自动映射，列名和属性名一致
- `resultMap`：手动映射，可以处理复杂情况

---

## 总结

本文我们学习了：

✅ MyBatis 的核心优势
✅ 环境搭建和配置
✅ 第一个 MyBatis 程序
✅ 核心概念理解

---

## 下一步

在下一篇文章中，我们将深入学习 **Mapper 配置与核心概念**，包括：

- 🎯 MyBatis 配置文件详解
- 🔧 Mapper 接口定义规范
- 📊 类型别名和类型处理器
- 🛠️ SqlSessionFactory 的高级配置

继续阅读：[02. Mapper 配置与核心概念](./02-mapper-configuration.mdx)
