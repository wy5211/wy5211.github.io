---
title: "03. 动态 SQL 详解：if、where、foreach 实战"
date: "2026-03-23"
summary: "掌握 MyBatis 动态 SQL 的使用，学会处理复杂的查询条件。"
tags: ["MyBatis", "动态SQL"]
category: "mybatis"
cover: ""
draft: false
series: "MyBatis 渐进式学习路线"
seriesOrder: 3
---

## 前言

实际业务中，查询条件往往是动态的。MyBatis 的动态 SQL 功能可以完美解决这个问题。

---

## if 标签

```xml
<select id="findUsers" resultType="User">
    SELECT * FROM users
    <where>
        <if test="username != null">
            AND username = #{username}
        </if>
        <if test="email != null">
            AND email = #{email}
        </if>
    </where>
</select>
```

---

## where/set 标签

```xml
<!-- where 标签 -->
<select id="findUsers" resultType="User">
    SELECT * FROM users
    <where>
        <if test="username != null">
            AND username = #{username}
        </if>
    </where>
</select>

<!-- set 标签 -->
<update id="updateUser">
    UPDATE users
    <set>
        <if test="username != null">
            username = #{username},
        </if>
    </set>
    WHERE id = #{id}
</update>
```

---

## foreach 标签

```xml
<!-- IN 查询 -->
<select id="selectByIds" resultType="User">
    SELECT * FROM users
    WHERE id IN
    <foreach collection="list" item="id" open="(" close=")" separator=",">
        #{id}
    </foreach>
</select>

<!-- 批量插入 -->
<insert id="batchInsert">
    INSERT INTO users (username, email) VALUES
    <foreach collection="list" item="user" separator=",">
        (#{user.username}, #{user.email})
    </foreach>
</insert>
```

---

## choose/when/otherwise

```xml
<select id="findUsers" resultType="User">
    SELECT * FROM users
    <where>
        <choose>
            <when test="id != null">
                id = #{id}
            </when>
            <when test="username != null">
                username = #{username}
            </when>
            <otherwise>
                1=1
            </otherwise>
        </choose>
    </where>
</select>
```

---

## 实战案例

### 复杂查询

```xml
<select id="findUsersByCondition" resultType="User">
    SELECT * FROM users
    <where>
        <if test="username != null and username != ''">
            <bind name="pattern" value="'%' + username + '%'" />
            AND username LIKE #{pattern}
        </if>
        <if test="minAge != null">
            AND age &gt;= #{minAge}
        </if>
        <if test="maxAge != null">
            AND age &lt;= #{maxAge}
        </if>
    </where>
    ORDER BY id
</select>
```

---

## 最佳实践

✅ 使用 where/set 自动处理
✅ 复用 SQL 片段
✅ 合理使用 foreach

---

## 总结

✅ if 条件判断
✅ where/set 标签
✅ foreach 循环
✅ choose 分支

---

## 下一步

继续阅读：[04. 结果映射深入](./04-result-mapping.mdx)
