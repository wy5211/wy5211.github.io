---
title: "10. MyBatis-Plus 增强工具：CRUD 增强"
date: "2026-03-23"
summary: "掌握 MyBatis-Plus 的使用，简化 MyBatis 开发，提升效率。"
tags: ["MyBatis", "MyBatis-Plus"]
category: "mybatis"
cover: ""
draft: false
series: "MyBatis 渐进式学习路线"
seriesOrder: 10
---

## 前言

MyBatis-Plus 是 MyBatis 的增强工具，大大简化了 CRUD 操作。

---

## 添加依赖

```xml
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-boot-starter</artifactId>
    <version>3.5.4</version>
</dependency>
```

---

## 配置

### application.yml

```yaml
mybatis-plus:
  # Mapper 文件位置
  mapper-locations: classpath:mapper/**/*.xml
  # 实体类别名包
  type-aliases-package: com.example.entity
  configuration:
    # 开启驼峰命名
    map-underscore-to-camel-case: true
    # 日志输出
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 主键类型
      id-type: auto
      # 逻辑删除字段
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
```

---

## CRUD 接口

### Mapper 接口

```java
@Mapper
public interface UserMapper extends BaseMapper<User> {
    // 继承 BaseMapper 后自动拥有 CRUD 方法
}
```

### Service 使用

```java
@Service
public class UserService {

    @Autowired
    private UserMapper userMapper;

    // 插入
    public void save(User user) {
        userMapper.insert(user);
    }

    // 根据 ID 查询
    public User getById(Long id) {
        return userMapper.selectById(id);
    }

    // 查询所有
    public List<User> getAll() {
        return userMapper.selectList(null);
    }

    // 更新
    public void update(User user) {
        userMapper.updateById(user);
    }

    // 删除
    public void delete(Long id) {
        userMapper.deleteById(id);
    }

    // 批量删除
    public void deleteBatch(List<Long> ids) {
        userMapper.deleteBatchIds(ids);
    }
}
```

---

## 条件构造器

### QueryWrapper

```java
public List<User> findUsers(String username, Integer minAge) {
    LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();

    // 条件
    wrapper.eq(StringUtils.isNotBlank(username), User::getUsername, username)
           .ge(minAge != null, User::getAge, minAge)
           .orderByDesc(User::getId);

    return userMapper.selectList(wrapper);
}
```

### UpdateWrapper

```java
public void updateSelective(Long id, String username) {
    LambdaUpdateWrapper<User> wrapper = new LambdaUpdateWrapper<>();

    wrapper.eq(User::getId, id)
           .set(User::getUsername, username);

    userMapper.update(null, wrapper);
}
```

---

## 最佳实践

✅ 使用 Lambda 表达式
✅ 复杂 SQL 用 XML
✅ 代码生成器

---

## 总结

✅ BaseMapper 接口
✅ LambdaQueryWrapper 条件构造
✅ 简化 CRUD 操作

---

## 下一步

继续阅读：[11. MyBatis 性能优化](./11-performance-optimization.mdx)
