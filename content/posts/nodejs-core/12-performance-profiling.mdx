---
title: "12 Node.js 性能监控：诊断工具与性能分析"
date: "2026-04-08"
summary: "深入掌握 Node.js 性能监控工具，使用 PerformanceObserver、V8 堆内存分析、CPU Profiling、Flame Graph 等技术诊断性能问题"
tags: ["Node.js", "性能监控", "Profiling", "诊断"]
category: "nodejs-core"
cover: ""
draft: false
series: "Node.js 核心概念深度解析"
seriesOrder: 12
---

## 前言

在 Node.js 后端开发中，**性能监控**是优化应用的关键。理解性能瓶颈、内存泄漏、CPU 使用等指标对于构建高性能应用至关重要。

**核心内容**：

- PerformanceObserver 与 performance.mark
- V8 堆内存分析
- CPU Profiling 与 Flame Graph
- 实战：构建性能监控系统

---

## 一、性能监控基础

### 1.1 PerformanceObserver

```javascript
const { performance, PerformanceObserver } = require("perf_hooks");

const obs = new PerformanceObserver((list) => {
  const entries = list.getEntries();
  entries.forEach((entry) => {
    console.log(`${entry.name}: ${entry.duration}ms`);
  });
});

obs.observe({ entryTypes: ["measure"] });

performance.mark("start");

// 执行代码
for (let i = 0; i < 1000000; i++) {
  Math.sqrt(i);
}

performance.mark("end");
performance.measure("loop", "start", "end");
```

### 1.2 内存监控

```javascript
const used = process.memoryUsage();

console.log({
  rss: (used.rss / 1024 / 1024).toFixed(2) + " MB",
  heapTotal: (used.heapTotal / 1024 / 1024).toFixed(2) + " MB",
  heapUsed: (used.heapUsed / 1024 / 1024).toFixed(2) + " MB",
  external: (used.external / 1024 / 1024).toFixed(2) + " MB",
});
```

---

## 二、CPU Profiling

### 2.1 生成 Profile

```bash
node --prof app.js
```

### 2.2 分析 Profile

```bash
node --prof-process isolate-0xnnnnnnnnnnnn-v8.log > profile.txt
```

### 2.3 使用 clinic.js

```bash
npm install -g clinic

clinic doctor -- node app.js
```

---

## 三、内存分析

### 3.1 堆快照

```javascript
const v8 = require("v8");

// 生成堆快照
const snapshot = v8.writeHeapSnapshot();
console.log("Heap snapshot:", snapshot);
```

### 3.2 内存泄漏检测

```javascript
const { performance } = require("perf_hooks");

setInterval(() => {
  const used = process.memoryUsage();
  console.log("Heap used:", (used.heapUsed / 1024 / 1024).toFixed(2), "MB");
}, 5000);
```

---

## 四、实战案例

### 4.1 性能监控系统

```javascript
const { performance, PerformanceObserver } = require("perf_hooks");

class PerformanceMonitor {
  constructor() {
    this.metrics = {};

    const obs = new PerformanceObserver((list) => {
      const entries = list.getEntries();
      entries.forEach((entry) => {
        this.metrics[entry.name] = entry.duration;
      });
    });

    obs.observe({ entryTypes: ["measure"] });
  }

  start(name) {
    performance.mark(`${name}-start`);
  }

  end(name) {
    performance.mark(`${name}-end`);
    performance.measure(name, `${name}-start`, `${name}-end`);
  }

  getMetrics() {
    return this.metrics;
  }
}

const monitor = new PerformanceMonitor();

monitor.start("api-call");
// API 调用
monitor.end("api-call");

console.log(monitor.getMetrics());
```

---

## 五、总结

**核心要点**：

1. PerformanceObserver 监控性能指标
2. V8 堆内存分析
3. CPU Profiling 定位瓶颈
4. clinic.js 可视化分析

**下一步**：调试与源码映射

---

**相关代码**：[nodejs-core-examples](https://github.com/your-repo/nodejs-core-examples)
