---
title: "15 Node.js 部署与运维：最佳实践"
date: "2026-04-11"
summary: "深入掌握 Node.js 部署与运维，使用 PM2、Docker、Kubernetes 进行生产级部署，实现日志收集、健康检查、优雅关闭"
tags: ["Node.js", "部署", "PM2", "Docker", "Kubernetes"]
category: "nodejs-core"
cover: ""
draft: false
series: "Node.js 核心概念深度解析"
seriesOrder: 15
---

## 前言

在 Node.js 后端开发中，**部署与运维**是上线生产的关键。理解 PM2、Docker、Kubernetes、日志收集、健康检查等技术对于构建生产级应用至关重要。

**核心内容**：

- PM2 进程管理与集群模式
- Dockerfile 多阶段构建优化
- Kubernetes Deployment 与 Service
- 日志收集（ELK/Loki）
- 健康检查与优雅关闭
- 实战：从零部署生产级 Node.js 应用

---

## 一、PM2 部署

### 1.1 基础使用

```bash
npm install -g pm2

pm2 start app.js
pm2 logs
pm2 stop all
pm2 delete all
```

### 1.2 集群模式

```javascript
// ecosystem.config.js
module.exports = {
  apps: [
    {
      script: "app.js",
      instances: "max",
      exec_mode: "cluster",
    },
  ],
};
```

```bash
pm2 start ecosystem.config.js
```

---

## 二、Docker 部署

### 2.1 Dockerfile

```dockerfile
FROM node:18-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

FROM node:18-alpine

WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules

EXPOSE 3000
CMD ["node", "dist/index.js"]
```

### 2.2 构建与运行

```bash
docker build -t my-app .
docker run -p 3000:3000 my-app
```

---

## 三、Kubernetes 部署

### 3.1 Deployment

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        - name: my-app
          image: my-app:latest
          ports:
            - containerPort: 3000
```

### 3.2 Service

```yaml
apiVersion: v1
kind: Service
metadata:
  name: my-app
spec:
  selector:
    app: my-app
  ports:
    - port: 80
      targetPort: 3000
  type: LoadBalancer
```

---

## 四、日志收集

### 4.1 Winston

```javascript
const winston = require("winston");

const logger = winston.createLogger({
  level: "info",
  format: winston.format.json(),
  transports: [
    new winston.transports.File({ filename: "error.log", level: "error" }),
    new winston.transports.File({ filename: "combined.log" }),
  ],
});
```

---

## 五、健康检查

### 5.1 端点

```javascript
app.get("/health", (req, res) => {
  res.json({ status: "ok" });
});
```

### 5.2 Kubernetes Liveness

```yaml
livenessProbe:
  httpGet:
    path: /health
    port: 3000
  initialDelaySeconds: 30
  periodSeconds: 10
```

---

## 六、总结

**核心要点**：

1. PM2 进程管理
2. Docker 容器化
3. Kubernetes 编排
4. 日志收集
5. 健康检查

---

**相关代码**：[nodejs-core-examples](https://github.com/your-repo/nodejs-core-examples)

**专栏完结！**
