---
title: "11 Node.js 错误处理：异步异常捕获与全局处理"
date: "2026-04-07"
summary: "深入掌握 Node.js 错误处理机制，理解 try-catch、unhandledRejection、async_hooks，实现全局错误处理和错误日志上报"
tags: ["Node.js", "错误处理", "异步", "异常捕获"]
category: "nodejs-core"
cover: ""
draft: false
series: "Node.js 核心概念深度解析"
seriesOrder: 11
---

## 前言

在 Node.js 后端开发中，**错误处理**是构建稳定应用的关键。异步操作的错误处理尤其重要，因为未捕获的错误会导致整个进程崩溃。

**核心内容**：

- Error 对象的 stack 与 code 属性
- try-catch 与 unhandledRejection
- async_hooks 追踪异步调用
- 实战：全局错误处理、错误日志上报

---

## 一、Error 对象

### 1.1 基础 Error

```javascript
const error = new Error("Something went wrong");

console.log(error.message); // 'Something went wrong'
console.log(error.stack); // 堆栈信息
console.log(error.code); // 错误码（如果有）
```

### 1.2 自定义 Error

```javascript
class AppError extends Error {
  constructor(message, code, statusCode) {
    super(message);
    this.code = code;
    this.statusCode = statusCode;
    this.isOperational = true;
    Error.captureStackTrace(this, this.constructor);
  }
}

const error = new AppError("Not found", "NOT_FOUND", 404);
console.log(error.code, error.statusCode);
```

---

## 二、try-catch

### 2.1 同步错误

```javascript
try {
  JSON.parse("invalid json");
} catch (error) {
  console.error("Error:", error.message);
}
```

### 2.2 异步错误

```javascript
// async/await
async function main() {
  try {
    const data = await fs.promises.readFile("file.txt");
  } catch (error) {
    console.error("Error:", error.message);
  }
}

// Promise
fs.promises
  .readFile("file.txt")
  .then((data) => console.log(data))
  .catch((error) => console.error("Error:", error.message));
```

---

## 三、全局错误处理

### 3.1 uncaughtException

```javascript
process.on("uncaughtException", (error) => {
  console.error("Uncaught Exception:", error);
  // 清理资源
  process.exit(1);
});
```

### 3.2 unhandledRejection

```javascript
process.on("unhandledRejection", (reason, promise) => {
  console.error("Unhandled Rejection at:", promise, "reason:", reason);
});
```

---

## 四、实战案例

### 4.1 全局错误中间件

```javascript
const express = require("express");

const app = express();

// 错误中间件
app.use((err, req, res, next) => {
  console.error("Error:", err);

  res.status(err.statusCode || 500).json({
    error: {
      message: err.message,
      code: err.code || "INTERNAL_ERROR",
    },
  });
});

app.get("/", (req, res) => {
  res.send("Hello");
});

app.listen(3000);
```

### 4.2 错误日志上报

```javascript
const winston = require("winston");

const logger = winston.createLogger({
  level: "error",
  format: winston.format.json(),
  transports: [new winston.transports.File({ filename: "error.log" })],
});

function logError(error) {
  logger.error({
    message: error.message,
    stack: error.stack,
    code: error.code,
  });
}
```

---

## 五、总结

**核心要点**：

1. try-catch 捕获同步错误
2. unhandledRejection 处理未捕获的 Promise 错误
3. 全局错误处理中间件
4. 错误日志上报

**下一步**：性能监控与诊断工具

---

**相关代码**：[nodejs-core-examples](https://github.com/your-repo/nodejs-core-examples)
