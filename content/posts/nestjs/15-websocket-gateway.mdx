---
title: "15 NestJS WebSocket 实时通信：构建实时应用"
date: "2026-03-19"
summary: "使用 NestJS Gateway 实现 WebSocket 实时通信，构建聊天室、实时通知等功能"
tags: ["NestJS", "WebSocket", "实时通信", "Socket.IO", "聊天"]
category: "nestjs"
cover: ""
draft: false
series: "NestJS 渐进式博客系统"
seriesOrder: 15
---

## 前言

实时通信是现代应用的重要功能。NestJS 通过 Gateway 提供了 WebSocket 支持。

**本文将带你：**

1. 配置 WebSocket
2. 创建 Gateway
3. 实现聊天室
4. 广播和定向推送
5. 房间管理

---

## 一、安装依赖

```bash
pnpm add @nestjs/websockets @nestjs/platform-socket.io socket.io
```

---

## 二、创建 Gateway

### 2.1 基础 Gateway

```typescript
import {
  WebSocketGateway,
  SubscribeMessage,
  MessageBody,
} from "@nestjs/websockets";
import { Logger } from "@nestjs/common";

@WebSocketGateway({
  cors: {
    origin: "*",
  },
})
export class ChatGateway {
  private readonly logger = new Logger(ChatGateway.name);

  @SubscribeMessage("message")
  handleMessage(@MessageBody() data: string): void {
    this.logger.log(`收到消息: ${data}`);
    return data;
  }
}
```

### 2.2 处理连接

```typescript
import {
  WebSocketGateway,
  WebSocketServer,
  SubscribeMessage,
  OnGatewayInit,
  OnGatewayConnection,
  OnGatewayDisconnect,
} from "@nestjs/websockets";
import { Server, Socket } from "socket.io";

@WebSocketGateway({ cors: { origin: "*" } })
export class ChatGateway
  implements OnGatewayInit, OnGatewayConnection, OnGatewayDisconnect
{
  @WebSocketServer()
  server: Server;

  private logger: Logger = new Logger(ChatGateway.name);

  afterInit(server: Server) {
    this.logger.log("WebSocket 初始化完成");
  }

  handleConnection(client: Socket) {
    this.logger.log(`客户端连接: ${client.id}`);
  }

  handleDisconnect(client: Socket) {
    this.logger.log(`客户端断开: ${client.id}`);
  }

  @SubscribeMessage("message")
  handleMessage(client: Socket, data: string): void {
    this.server.emit("message", data);
  }
}
```

---

## 三、聊天室实现

### 3.1 加入房间

```typescript
@WebSocketGateway({ cors: { origin: "*" } })
export class ChatGateway {
  private rooms: Map<string, Set<string>> = new Map();

  @SubscribeMessage("joinRoom")
  handleJoinRoom(client: Socket, room: string) {
    client.join(room);

    if (!this.rooms.has(room)) {
      this.rooms.set(room, new Set());
    }
    this.rooms.get(room)!.add(client.id);

    client.emit("joined", { room });
    this.server.to(room).emit("userJoined", { userId: client.id });
  }

  @SubscribeMessage("leaveRoom")
  handleLeaveRoom(client: Socket, room: string) {
    client.leave(room);

    this.rooms.get(room)?.delete(client.id);
    this.server.to(room).emit("userLeft", { userId: client.id });
  }
}
```

### 3.2 发送消息

```typescript
@SubscribeMessage('sendMessage')
 handleMessage(client: Socket, payload: { room: string; message: string }) {
  const { room, message } = payload;

  // 广播给房间内其他用户
  client.to(room).emit('message', {
    userId: client.id,
    message,
    timestamp: new Date(),
  });

  // 确认发送者
  client.emit('messageSent', { message });
}
```

---

## 四、总结

**核心要点**：

1. **@WebSocketGateway** - WebSocket 装饰器
2. **@SubscribeMessage** - 订阅消息
3. **房间** - 消息分组
4. **广播** - 向所有客户端发送
5. **定向推送** - 向特定客户端发送

**下一步**：《测试策略》

---
