---
title: "17 NestJS API 文档：Swagger 自动化文档生成"
date: "2026-03-19"
summary: "使用 Swagger 自动生成 API 文档，提供在线测试接口，提升开发效率"
tags: ["NestJS", "Swagger", "API文档", "OpenAPI", "文档"]
category: "nestjs"
cover: ""
draft: false
series: "NestJS 渐进式博客系统"
seriesOrder: 17
---

## 前言

API 文档是前后端协作的重要工具。NestJS 可以通过 Swagger 自动生成交互式文档。

**本文将带你：**

1. 配置 Swagger
2. 添加 API 文档注解
3. 定义 Schema
4. 配置认证
5. 自定义文档

---

## 一、安装依赖

```bash
pnpm add @nestjs/swagger
```

---

## 二、配置 Swagger

### 2.1 基础配置

```typescript
import { NestFactory } from "@nestjs/core";
import { SwaggerModule, DocumentBuilder } from "@nestjs/swagger";
import { AppModule } from "./app.module";

async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  const config = new DocumentBuilder()
    .setTitle("博客 API")
    .setDescription("博客系统 API 文档")
    .setVersion("1.0")
    .addTag("users", "用户管理")
    .addTag("posts", "文章管理")
    .addBearerAuth()
    .build();

  const document = SwaggerModule.createDocument(app, config);
  SwaggerModule.setup("api-docs", app, document);

  await app.listen(3000);
}
bootstrap();
```

### 2.2 访问文档

启动应用后，访问 `http://localhost:3000/api-docs`

---

## 三、添加注解

### 3.1 DTO 注解

```typescript
import { ApiProperty } from "@nestjs/swagger";

export class CreateUserDto {
  @ApiProperty({ example: "zhangsan@example.com", description: "邮箱" })
  @IsEmail()
  email: string;

  @ApiProperty({ example: "张三", description: "用户名" })
  @IsString()
  name: string;

  @ApiProperty({ example: "Password123!", description: "密码" })
  @IsString()
  @MinLength(8)
  password: string;
}
```

### 3.2 控制器注解

```typescript
import {
  ApiTags,
  ApiOperation,
  ApiResponse,
  ApiBearerAuth,
} from "@nestjs/swagger";

@ApiTags("users")
@ApiBearerAuth()
@Controller("users")
export class UsersController {
  @Get()
  @ApiOperation({ summary: "获取所有用户" })
  @ApiResponse({ status: 200, description: "成功", type: [User] })
  findAll() {
    return this.usersService.findAll();
  }

  @Post()
  @ApiOperation({ summary: "创建用户" })
  @ApiResponse({ status: 201, description: "创建成功", type: User })
  create(@Body() dto: CreateUserDto) {
    return this.usersService.create(dto);
  }
}
```

---

## 四、总结

**核心要点**：

1. **Swagger** - API 文档生成
2. **@ApiProperty** - 属性注解
3. **@ApiOperation** - 操作描述
4. **@ApiResponse** - 响应说明
5. **交互式测试** - 在线测试接口

**下一步**：《Docker 部署》

---
