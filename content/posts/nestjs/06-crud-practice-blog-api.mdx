---
title: "06 CRUD 实战：构建完整的博客文章 API"
date: "2026-03-19"
summary: "整合前面所学知识，从零开始构建一个完整的博客文章 CRUD API，包含数据验证、统一响应格式、错误处理等生产级实践"
tags: ["NestJS", "CRUD", "实战", "API", "PostgreSQL"]
category: "nestjs"
cover: ""
draft: false
series: "NestJS 渐进式博客系统"
seriesOrder: 6
---

## 前言

在前面五篇文章中，我们学习了 NestJS 的核心概念、模块化架构、控制器路由、服务层设计，以及 PostgreSQL + Prisma 的使用。

**本文将做什么？**

我们将整合前面学到的所有知识，**从零开始构建一个生产级的博客文章 CRUD API**。

**本文将带你：**

1. 完整实现文章的增删改查功能
2. 添加数据验证（DTO + Pipes）
3. 实现统一的响应格式
4. 添加错误处理和异常过滤器
5. 实现文章发布、搜索等高级功能

---

## 一、项目准备

### 1.1 初始化项目

```bash
# 创建项目
nest new blog-api -p pnpm
cd blog-api

# 安装依赖
pnpm add @prisma/client @nestjs/config class-validator class-transformer
pnpm add -D prisma

# 初始化 Prisma
npx prisma init
```

### 1.2 配置数据库

`.env`:

```env
DATABASE_URL="postgresql://blog:blog123@localhost:5432/blog_api?schema=public"
```

`docker-compose.yml`:

```yaml
version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: blog-postgres
    environment:
      POSTGRES_USER: blog
      POSTGRES_PASSWORD: blog123
      POSTGRES_DB: blog_api
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

启动数据库：

```bash
docker-compose up -d
```

### 1.3 定义数据模型

`prisma/schema.prisma`:

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  name      String
  role      Role     @default(USER)

  posts     Post[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")
}

model Post {
  id          String    @id @default(uuid())
  title       String
  content     String?   @db.Text
  excerpt     String?   @db.Text
  coverImage  String?
  slug        String?   @unique
  published   Boolean   @default(false)
  views       Int       @default(0)

  authorId    String
  author      User      @relation(fields: [authorId], references: [id], onDelete: Cascade)

  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  publishedAt DateTime?

  @@map("posts")
  @@index([authorId])
  @@index([published])
}

enum Role {
  USER
  ADMIN
}
```

创建数据库和迁移：

```bash
npx prisma migrate dev --name init
```

---

## 二、创建数据库模块

### 2.1 Prisma Service

`src/database/prisma.service.ts`:

```typescript
import { Injectable, OnModuleInit, OnModuleDestroy } from "@nestjs/common";
import { PrismaClient } from "@prisma/client";

@Injectable()
export class PrismaService
  extends PrismaClient
  implements OnModuleInit, OnModuleDestroy
{
  async onModuleInit() {
    await this.$connect();
  }

  async onModuleDestroy() {
    await this.$disconnect();
  }
}
```

### 2.2 Database Module

`src/database/database.module.ts`:

```typescript
import { Global, Module } from "@nestjs/common";
import { PrismaService } from "./prisma.service";

@Global()
@Module({
  providers: [PrismaService],
  exports: [PrismaService],
})
export class DatabaseModule {}
```

---

## 三、创建 DTO（数据传输对象）

### 3.1 创建文章 DTO

`src/posts/dto/create-post.dto.ts`:

```typescript
import { ApiProperty } from "@nestjs/swagger";
import {
  IsString,
  IsNotEmpty,
  IsOptional,
  MaxLength,
  IsUrl,
} from "class-validator";

export class CreatePostDto {
  @ApiProperty({ example: "如何学习 NestJS" })
  @IsString()
  @IsNotEmpty()
  @MaxLength(200)
  title: string;

  @ApiProperty({
    example: "这是一篇关于 NestJS 的入门教程...",
    required: false,
  })
  @IsOptional()
  @IsString()
  content?: string;

  @ApiProperty({
    example: "本文介绍 NestJS 的核心概念...",
    required: false,
  })
  @IsOptional()
  @IsString()
  @MaxLength(500)
  excerpt?: string;

  @ApiProperty({
    example: "https://example.com/image.jpg",
    required: false,
  })
  @IsOptional()
  @IsUrl()
  coverImage?: string;
}
```

### 3.2 更新文章 DTO

`src/posts/dto/update-post.dto.ts`:

```typescript
import { PartialType } from "@nestjs/swagger";
import { CreatePostDto } from "./create-post.dto";

export class UpdatePostDto extends PartialType(CreatePostDto) {}
```

### 3.3 查询文章 DTO

`src/posts/dto/query-posts.dto.ts`:

```typescript
import { ApiProperty } from "@nestjs/swagger";
import { IsOptional, IsInt, Min, IsBoolean, IsString } from "class-validator";
import { Type } from "class-transformer";

export class QueryPostsDto {
  @ApiProperty({ required: false, default: 1 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  page?: number = 1;

  @ApiProperty({ required: false, default: 10 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  limit?: number = 10;

  @ApiProperty({ required: false })
  @IsOptional()
  @IsString()
  search?: string;

  @ApiProperty({ required: false })
  @IsOptional()
  @IsBoolean()
  published?: boolean;
}
```

---

## 四、创建文章服务

`src/posts/posts.service.ts`:

```typescript
import {
  Injectable,
  NotFoundException,
  ForbiddenException,
} from "@nestjs/common";
import { PrismaService } from "../database/prisma.service";
import { CreatePostDto } from "./dto/create-post.dto";
import { UpdatePostDto } from "./dto/update-post.dto";

@Injectable()
export class PostsService {
  constructor(private prisma: PrismaService) {}

  /**
   * 创建文章
   */
  async create(userId: string, dto: CreatePostDto) {
    const slug = this.generateSlug(dto.title);

    return this.prisma.post.create({
      data: {
        ...dto,
        slug,
        authorId: userId,
      },
      include: {
        author: {
          select: { id: true, name: true, email: true },
        },
      },
    });
  }

  /**
   * 获取所有文章
   */
  async findAll(query: any) {
    const { page = 1, limit = 10, search, published } = query;
    const skip = (page - 1) * limit;

    const where: any = {};

    if (search) {
      where.OR = [
        { title: { contains: search, mode: "insensitive" } },
        { content: { contains: search, mode: "insensitive" } },
      ];
    }

    if (published !== undefined) {
      where.published = published === "true" || published === true;
    }

    const [posts, total] = await Promise.all([
      this.prisma.post.findMany({
        where,
        skip,
        take: Number(limit),
        orderBy: { createdAt: "desc" },
        include: {
          author: {
            select: { id: true, name: true, email: true },
          },
        },
      }),
      this.prisma.post.count({ where }),
    ]);

    return {
      data: posts,
      meta: {
        total,
        page: Number(page),
        limit: Number(limit),
        totalPages: Math.ceil(total / Number(limit)),
      },
    };
  }

  /**
   * 获取单篇文章
   */
  async findOne(id: string) {
    const post = await this.prisma.post.findUnique({
      where: { id },
      include: {
        author: {
          select: { id: true, name: true, email: true },
        },
      },
    });

    if (!post) {
      throw new NotFoundException("文章不存在");
    }

    // 增加阅读量
    await this.prisma.post.update({
      where: { id },
      data: { views: { increment: 1 } },
    });

    return post;
  }

  /**
   * 更新文章
   */
  async update(id: string, userId: string, dto: UpdatePostDto) {
    const post = await this.prisma.post.findUnique({
      where: { id },
    });

    if (!post) {
      throw new NotFoundException("文章不存在");
    }

    if (post.authorId !== userId) {
      throw new ForbiddenException("无权修改此文章");
    }

    if (dto.title) {
      dto.slug = this.generateSlug(dto.title);
    }

    return this.prisma.post.update({
      where: { id },
      data: dto,
      include: {
        author: {
          select: { id: true, name: true, email: true },
        },
      },
    });
  }

  /**
   * 删除文章
   */
  async remove(id: string, userId: string) {
    const post = await this.prisma.post.findUnique({
      where: { id },
    });

    if (!post) {
      throw new NotFoundException("文章不存在");
    }

    if (post.authorId !== userId) {
      throw new ForbiddenException("无权删除此文章");
    }

    await this.prisma.post.delete({
      where: { id },
    });

    return { message: "删除成功" };
  }

  /**
   * 发布文章
   */
  async publish(id: string, userId: string) {
    const post = await this.prisma.post.findUnique({
      where: { id },
    });

    if (!post) {
      throw new NotFoundException("文章不存在");
    }

    if (post.authorId !== userId) {
      throw new ForbiddenException("无权操作此文章");
    }

    return this.prisma.post.update({
      where: { id },
      data: {
        published: true,
        publishedAt: new Date(),
      },
    });
  }

  /**
   * 取消发布
   */
  async unpublish(id: string, userId: string) {
    const post = await this.prisma.post.findUnique({
      where: { id },
    });

    if (!post) {
      throw new NotFoundException("文章不存在");
    }

    if (post.authorId !== userId) {
      throw new ForbiddenException("无权操作此文章");
    }

    return this.prisma.post.update({
      where: { id },
      data: {
        published: false,
        publishedAt: null,
      },
    });
  }

  private generateSlug(title: string): string {
    return title
      .toLowerCase()
      .trim()
      .replace(/\s+/g, "-")
      .replace(/[^\w\-]+/g, "")
      .replace(/\-\-+/g, "-");
  }
}
```

---

## 五、创建文章控制器

`src/posts/posts.controller.ts`:

```typescript
import {
  Controller,
  Get,
  Post,
  Put,
  Patch,
  Delete,
  Param,
  Query,
  Body,
  UseGuards,
  HttpCode,
  HttpStatus,
} from "@nestjs/common";
import { PostsService } from "./posts.service";
import { CreatePostDto } from "./dto/create-post.dto";
import { UpdatePostDto } from "./dto/update-post.dto";
import { QueryPostsDto } from "./dto/query-posts.dto";
import { JwtAuthGuard } from "../auth/guards/jwt-auth.guard";
import { CurrentUser } from "../auth/decorators/current-user.decorator";

@Controller("posts")
export class PostsController {
  constructor(private postsService: PostsService) {}

  /**
   * 获取所有文章
   */
  @Get()
  async findAll(@Query() query: QueryPostsDto) {
    return this.postsService.findAll(query);
  }

  /**
   * 获取单篇文章
   */
  @Get(":id")
  async findOne(@Param("id") id: string) {
    return this.postsService.findOne(id);
  }

  /**
   * 创建文章
   */
  @Post()
  @UseGuards(JwtAuthGuard)
  @HttpCode(HttpStatus.CREATED)
  async create(
    @CurrentUser("userId") userId: string,
    @Body() createPostDto: CreatePostDto
  ) {
    return this.postsService.create(userId, createPostDto);
  }

  /**
   * 更新文章
   */
  @Patch(":id")
  @UseGuards(JwtAuthGuard)
  async update(
    @Param("id") id: string,
    @CurrentUser("userId") userId: string,
    @Body() updatePostDto: UpdatePostDto
  ) {
    return this.postsService.update(id, userId, updatePostDto);
  }

  /**
   * 删除文章
   */
  @Delete(":id")
  @UseGuards(JwtAuthGuard)
  @HttpCode(HttpStatus.NO_CONTENT)
  async remove(@Param("id") id: string, @CurrentUser("userId") userId: string) {
    await this.postsService.remove(id, userId);
  }

  /**
   * 发布文章
   */
  @Post(":id/publish")
  @UseGuards(JwtAuthGuard)
  async publish(
    @Param("id") id: string,
    @CurrentUser("userId") userId: string
  ) {
    return this.postsService.publish(id, userId);
  }

  /**
   * 取消发布
   */
  @Post(":id/unpublish")
  @UseGuards(JwtAuthGuard)
  async unpublish(
    @Param("id") id: string,
    @CurrentUser("userId") userId: string
  ) {
    return this.postsService.unpublish(id, userId);
  }
}
```

---

## 六、统一响应格式

### 6.1 创建响应拦截器

`src/common/interceptors/transform.interceptor.ts`:

```typescript
import {
  Injectable,
  NestInterceptor,
  ExecutionContext,
  CallHandler,
} from "@nestjs/common";
import { Observable } from "rxjs";
import { map } from "rxjs/operators";

@Injectable()
export class TransformInterceptor<T> implements NestInterceptor<T, any> {
  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
    return next.handle().pipe(
      map((data) => {
        // 如果已经是标准格式，直接返回
        if (data && data.success !== undefined) {
          return data;
        }

        // 否则包装成标准格式
        return {
          success: true,
          data,
          timestamp: new Date().toISOString(),
        };
      })
    );
  }
}
```

### 6.2 全局异常过滤器

`src/common/filters/http-exception.filter.ts`:

```typescript
import {
  ExceptionFilter,
  Catch,
  ArgumentsHost,
  HttpException,
  HttpStatus,
} from "@nestjs/common";
import { Request, Response } from "express";

@Catch(HttpException)
export class HttpExceptionFilter implements ExceptionFilter {
  catch(exception: HttpException, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();
    const request = ctx.getRequest<Request>();
    const status = exception.getStatus();
    const exceptionResponse = exception.getResponse();

    const error =
      typeof exceptionResponse === "string"
        ? { message: exceptionResponse }
        : exceptionResponse;

    response.status(status).json({
      success: false,
      statusCode: status,
      timestamp: new Date().toISOString(),
      path: request.url,
      error: error,
    });
  }
}
```

### 6.3 在 APP 中注册

`src/main.ts`:

```typescript
import { ValidationPipe } from "@nestjs/common";
import { NestFactory } from "@nestjs/core";
import { AppModule } from "./app.module";
import { TransformInterceptor } from "./common/interceptors/transform.interceptor";
import { HttpExceptionFilter } from "./common/filters/http-exception.filter";

async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  // 全局验证管道
  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true, // 移除未在 DTO 中定义的属性
      forbidNonWhitelisted: true, // 如果有未定义的属性则抛出错误
      transform: true, // 自动转换类型
    })
  );

  // 全局响应拦截器
  app.useGlobalInterceptors(new TransformInterceptor());

  // 全局异常过滤器
  app.useGlobalFilters(new HttpExceptionFilter());

  await app.listen(3000);
}
bootstrap();
```

---

## 七、创建文章模块

`src/posts/posts.module.ts`:

```typescript
import { Module } from "@nestjs/common";
import { PostsService } from "./posts.service";
import { PostsController } from "./posts.controller";

@Module({
  controllers: [PostsController],
  providers: [PostsService],
  exports: [PostsService],
})
export class PostsModule {}
```

`src/app.module.ts`:

```typescript
import { Module } from "@nestjs/common";
import { ConfigModule } from "@nestjs/config";
import { DatabaseModule } from "./database/database.module";
import { PostsModule } from "./posts/posts.module";
import { AuthModule } from "./auth/auth.module";

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    DatabaseModule,
    PostsModule,
    AuthModule,
  ],
})
export class AppModule {}
```

---

## 八、测试 API

### 8.1 创建文章

```bash
curl -X POST http://localhost:3000/posts \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "title": "如何学习 NestJS",
    "content": "这是一篇详细的 NestJS 入门教程...",
    "excerpt": "本文介绍 NestJS 的核心概念",
    "coverImage": "https://example.com/image.jpg"
  }'
```

**响应**：

```json
{
  "success": true,
  "data": {
    "id": "uuid",
    "title": "如何学习 NestJS",
    "content": "这是一篇详细的 NestJS 入门教程...",
    "excerpt": "本文介绍 NestJS 的核心概念",
    "coverImage": "https://example.com/image.jpg",
    "slug": "如何学习-nestjs",
    "published": false,
    "views": 0,
    "authorId": "user-uuid",
    "author": {
      "id": "user-uuid",
      "name": "张三",
      "email": "zhangsan@example.com"
    },
    "createdAt": "2026-03-19T10:00:00.000Z",
    "updatedAt": "2026-03-19T10:00:00.000Z",
    "publishedAt": null
  },
  "timestamp": "2026-03-19T10:00:00.000Z"
}
```

### 8.2 获取所有文章

```bash
curl "http://localhost:3000/posts?page=1&limit=10&search=NestJS"
```

**响应**：

```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "title": "如何学习 NestJS",
      "excerpt": "本文介绍 NestJS 的核心概念",
      "published": false,
      "views": 100,
      "author": {
        "id": "user-uuid",
        "name": "张三",
        "email": "zhangsan@example.com"
      },
      "createdAt": "2026-03-19T10:00:00.000Z"
    }
  ],
  "meta": {
    "total": 1,
    "page": 1,
    "limit": 10,
    "totalPages": 1
  },
  "timestamp": "2026-03-19T10:00:00.000Z"
}
```

### 8.3 更新文章

```bash
curl -X PATCH http://localhost:3000/posts/uuid \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "title": "NestJS 完整指南",
    "published": false
  }'
```

### 8.4 发布文章

```bash
curl -X POST http://localhost:3000/posts/uuid/publish \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 8.5 删除文章

```bash
curl -X DELETE http://localhost:3000/posts/uuid \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 九、错误处理示例

### 9.1 验证错误

**请求**：

```bash
curl -X POST http://localhost:3000/posts \
  -H "Content-Type: application/json" \
  -d '{"title": ""}'
```

**响应**：

```json
{
  "success": false,
  "statusCode": 400,
  "timestamp": "2026-03-19T10:00:00.000Z",
  "path": "/posts",
  "error": {
    "message": [
      "title should not be empty",
      "title must be shorter than or equal to 200 characters"
    ],
    "error": "Bad Request"
  }
}
```

### 9.2 文章不存在

**响应**：

```json
{
  "success": false,
  "statusCode": 404,
  "timestamp": "2026-03-19T10:00:00.000Z",
  "path": "/posts/invalid-uuid",
  "error": {
    "message": "文章不存在",
    "error": "Not Found"
  }
}
```

### 9.3 无权限操作

**响应**：

```json
{
  "success": false,
  "statusCode": 403,
  "timestamp": "2026-03-19T10:00:00.000Z",
  "path": "/posts/uuid",
  "error": {
    "message": "无权修改此文章",
    "error": "Forbidden"
  }
}
```

---

## 十、总结

**本文我们完成了**：

✅ 从零开始构建了完整的博客文章 API
✅ 实现了 CRUD 基础功能
✅ 添加了数据验证
✅ 实现了统一响应格式
✅ 添加了错误处理
✅ 实现了文章发布、搜索等高级功能

**核心要点回顾**：

1. **DTO 验证** - 使用 class-validator 验证请求数据
2. **服务层** - 处理业务逻辑和数据操作
3. **控制器层** - 处理 HTTP 请求和响应
4. **统一响应** - 使用拦截器统一响应格式
5. **错误处理** - 使用异常过滤器统一错误处理

**最佳实践**：

- ✅ 使用 DTO 验证请求数据
- ✅ 保持控制器简洁，逻辑放在服务层
- ✅ 使用统一的响应格式
- ✅ 提供清晰的错误信息
- ✅ 使用 slug 生成 URL 友好的标识

**下一步**：

在下一篇文章《数据验证与管道》中，我们将深入学习：

- class-validator 的高级用法
- 自定义验证规则
- 自定义管道
- 全局验证配置

---

**相关代码**：

- 完整项目示例：[blog-api](https://github.com/your-repo/blog-api)
- 本篇代码示例：[branch: 06-crud-practice](https://github.com/your-repo/blog-api/tree/06-crud-practice)

**有问题？** 欢迎在评论区留言，我会尽快回复！
