---
title: "18 NestJS Docker 部署：容器化与生产优化"
date: "2026-03-19"
summary: "使用 Docker 容器化部署 NestJS 应用，实现多阶段构建、环境配置、反向代理等生产级部署方案"
tags: ["NestJS", "Docker", "部署", "Nginx", "DevOps"]
category: "nestjs"
cover: ""
draft: false
series: "NestJS 渐进式博客系统"
seriesOrder: 18
---

## 前言

这是本系列的最后一篇文章。我们将学习如何使用 Docker 部署 NestJS 应用到生产环境。

**本文将带你：**

1. 编写 Dockerfile
2. 多阶段构建优化
3. Docker Compose 配置
4. Nginx 反向代理
5. 生产环境最佳实践

---

## 一、Dockerfile

### 1.1 基础 Dockerfile

```dockerfile
# 构建阶段
FROM node:20-alpine AS builder

WORKDIR /app

# 安装 pnpm
RUN npm install -g pnpm

# 复制依赖文件
COPY package.json pnpm-lock.yaml ./

# 安装依赖
RUN pnpm install --frozen-lockfile

# 复制源代码
COPY . .

# 构建应用
RUN pnpm run build

# 生产阶段
FROM node:20-alpine AS production

WORKDIR /app

# 安装 pnpm
RUN npm install -g pnpm

# 复制依赖文件
COPY package.json pnpm-lock.yaml ./

# 只安装生产依赖
RUN pnpm install --prod --frozen-lockfile

# 从构建阶段复制构建产物
COPY --from=builder /app/dist ./dist

# 暴露端口
EXPOSE 3000

# 启动应用
CMD ["node", "dist/main"]
```

### 1.2 优化的 Dockerfile

```dockerfile
# 构建
FROM node:20-alpine AS build
WORKDIR /app

# 安装 pnpm
RUN npm install -g pnpm@8

# 复制依赖文件
COPY package.json pnpm-lock.yaml* ./

# 安装依赖
RUN pnpm install --frozen-lockfile

# 复制源代码
COPY . .

# 构建应用
RUN pnpm run build

# 生产镜像
FROM node:20-alpine AS production

# 添加非 root 用户
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nestjs -u 1001

WORKDIR /app

# 安装 pnpm
RUN npm install -g pnpm@8

# 复制依赖
COPY package.json pnpm-lock.yaml* ./
RUN pnpm install --prod --frozen-lockfile

# 复制构建产物
COPY --from=build /app/dist ./dist
COPY --from=build /app/node_modules/.prisma ./node_modules/.prisma

# 更改所有权
RUN chown -R nestjs:nodejs /app

USER nestjs

EXPOSE 3000

ENV NODE_ENV=production

CMD ["node", "dist/main"]
```

---

## 二、Docker Compose

### 2.1 完整配置

```yaml
version: "3.8"

services:
  # PostgreSQL
  postgres:
    image: postgres:16-alpine
    container_name: nestjs-postgres
    restart: always
    environment:
      POSTGRES_USER: nestjs
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: nestjs_blog
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nestjs"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis
  redis:
    image: redis:7-alpine
    container_name: nestjs-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # NestJS App
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nestjs-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://nestjs:${DB_PASSWORD}@postgres:5432/nestjs_blog
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: sh -c "npx prisma migrate deploy && node dist/main"

  # Nginx
  nginx:
    image: nginx:alpine
    container_name: nestjs-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - app

volumes:
  postgres_data:
  redis_data:
```

---

## 三、Nginx 配置

### 3.1 反向代理

```nginx
events {
    worker_connections 1024;
}

http {
    upstream nestjs {
        server app:3000;
    }

    # 重定向 HTTP 到 HTTPS
    server {
        listen 80;
        server_name example.com www.example.com;
        return 301 https://$server_name$request_uri;
    }

    # HTTPS 配置
    server {
        listen 443 ssl http2;
        server_name example.com www.example.com;

        # SSL 证书
        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;

        # SSL 优化
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;

        # 日志
        access_log /var/log/nginx/access.log;
        error_log /var/log/nginx/error.log;

        # 反向代理
        location / {
            proxy_pass http://nestjs;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;

            # 超时设置
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }

        # 静态文件缓存
        location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
}
```

---

## 四、环境变量

### 4.1 .env 文件

```env
# 数据库
DB_PASSWORD=your_strong_password

# JWT
JWT_SECRET=your_jwt_secret_change_this

# 应用
NODE_ENV=production
PORT=3000
```

---

## 五、部署流程

### 5.1 构建和启动

```bash
# 1. 构建镜像
docker-compose build

# 2. 启动服务
docker-compose up -d

# 3. 查看日志
docker-compose logs -f app

# 4. 执行数据库迁移
docker-compose exec app npx prisma migrate deploy
```

### 5.2 更新部署

```bash
# 1. 拉取最新代码
git pull

# 2. 重新构建
docker-compose build

# 3. 重启服务
docker-compose up -d

# 4. 清理旧镜像
docker image prune -a
```

---

## 六、生产优化

### 6.1 性能优化

```typescript
// main.ts
async function bootstrap() {
  const app = await NestFactory.create(AppModule, {
    logger: ["error", "warn"], // 生产环境只记录错误和警告
  });

  // 启用压缩
  app.use(compression());

  // 启用 CORS
  app.enableCors({
    origin: process.env.ALLOWED_ORIGINS?.split(","),
  });

  // 限流
  app.use(
    rateLimit({
      windowMs: 15 * 60 * 1000, // 15 分钟
      max: 100, // 最多 100 个请求
    })
  );

  await app.listen(3000);
}
```

### 6.2 健康检查

```typescript
@Controller("health")
export class HealthController {
  @Get()
  @Public()
  check() {
    return {
      status: "ok",
      timestamp: new Date().toISOString(),
    };
  }
}
```

---

## 七、总结

恭喜你完成了整个系列的学习！

**本系列回顾**：

1. ✅ 环境搭建与第一个项目
2. ✅ 模块与依赖注入
3. ✅ 控制器与路由
4. ✅ 提供者与服务
5. ✅ PostgreSQL + Prisma
6. ✅ CRUD 实战
7. ✅ 数据验证与管道
8. ✅ JWT 认证
9. ✅ 授权与守卫
10. ✅ 拦截器与日志
11. ✅ 异常处理
12. ✅ 文件上传
13. ✅ 异步任务队列
14. ✅ 缓存策略
15. ✅ WebSocket
16. ✅ 测试策略
17. ✅ Swagger 文档
18. ✅ Docker 部署

**你现在可以**：

- ✅ 独立搭建 NestJS 项目
- ✅ 设计 RESTful API
- ✅ 实现完整的认证授权
- ✅ 熟练使用 Prisma 操作数据库
- ✅ 处理文件上传、异步任务
- ✅ 编写测试和生成文档
- ✅ Docker 容器化部署

**继续学习**：

- NestJS 微服务
- GraphQL 集成
- Kubernetes 部署
- CI/CD 流程

**祝你学习愉快！**

---

**相关代码**：[完整项目](https://github.com/your-repo/blog-api)
