---
title: "10 事务与并发控制"
date: "2026-03-23"
summary: "掌握 MongoDB 事务处理，实现 ACID 事务和并发控制"
tags: ["Mongoose", "事务", "并发控制", "ACID"]
category: "mongoose"
draft: false
series: "Mongoose 渐进式学习"
seriesOrder: 10
---

## 前言

MongoDB 4.0+ 支持多文档 ACID 事务。本文将学习事务使用和并发控制。

## 事务基础

### 开启事务

```typescript
const session = await mongoose.startSession();
session.startTransaction();

try {
  // 操作
  await session.commitTransaction();
} catch (error) {
  await session.abortTransaction();
  throw error;
} finally {
  session.endSession();
}
```

### withTransaction

```typescript
const session = await mongoose.startSession();

try {
  await session.withTransaction(async () => {
    const user = await User.findById(userId).session(session);
    user.balance -= amount;
    await user.save({ session });

    await Transaction.create([{ userId, amount }], { session });
  });
} finally {
  session.endSession();
}
```

## 并发控制

### 乐观锁

```typescript
productSchema.pre("save", function (next) {
  if (!this.isNew) {
    this.increment();
  }
  next();
});

const product = await Product.findById(id);
product.stock -= quantity;
await product.save(); // version 冲突会失败
```

### 悲观锁

```typescript
const doc = await Model.findById(id).session(session).exec();
await doc.lock();
// 操作
await doc.save();
```

## 最佳实践

1. ✅ 事务尽可能短小
2. ✅ 使用重试逻辑
3. ✅ 注意性能影响
4. ✅ 错误处理和回滚

## 总结

1. ✅ ACID 事务
2. ✅ 并发控制
3. ✅ 乐观锁和悲观锁
4. ✅ 错误处理

---

- **上一篇**: [09 性能优化与索引](../09-performance.mdx)
- **下一篇**: [11 测试策略](../11-testing.mdx)
