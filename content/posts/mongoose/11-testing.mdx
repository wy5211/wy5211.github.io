---
title: "11 测试策略与实践"
date: "2026-03-23"
summary: "编写可靠的 Mongoose 测试，使用 mongodb-memory-server 进行单元测试和集成测试"
tags: ["Mongoose", "测试", "Jest", "mongodb-memory-server"]
category: "mongoose"
draft: false
series: "Mongoose 渐进式学习"
seriesOrder: 11
---

## 前言

测试是确保代码质量的关键。本文将学习 Mongoose 应用的测试策略。

## 测试环境

### mongodb-memory-server

```bash
npm install --save-dev mongodb-memory-server @types/mongodb-memory-server
```

## 测试示例

### Model 测试

```typescript
import { MongoMemoryServer } from "mongodb-memory-server";
import mongoose from "mongoose";
import { User, IUser } from "../models/User";

describe("User Model", () => {
  let mongod: MongoMemoryServer;
  let conn: typeof mongoose;

  beforeAll(async () => {
    mongod = await MongoMemoryServer.create();
    conn = await mongoose.createConnection(mongod.getUri());
  });

  afterAll(async () => {
    await conn.disconnect();
    await mongod.stop();
  });

  it("should create user with valid data", async () => {
    const user = await User.create({
      name: "张三",
      email: "zhang@example.com",
      password: "password123",
    });

    expect(user.name).toBe("张三");
    expect(user.email).toBe("zhang@example.com");
  });

  it("should fail validation for invalid email", async () => {
    await expect(
      User.create({
        name: "张三",
        email: "invalid-email",
        password: "password123",
      })
    ).rejects.toThrow();
  });
});
```

### 集成测试

```typescript
describe("Post CRUD", () => {
  it("should create and find post", async () => {
    const post = await Post.create({
      title: "Test Post",
      content: "Test content",
      author: userId,
    });

    const found = await Post.findById(post.id);
    expect(found.title).toBe("Test Post");
  });

  it("should update post", async () => {
    await Post.findByIdAndUpdate(
      postId,
      { $set: { title: "Updated" } },
      { new: true }
    );

    const updated = await Post.findById(postId);
    expect(updated.title).toBe("Updated");
  });
});
```

## 最佳实践

1. ✅ 使用内存数据库测试
2. ✅ 测试验证逻辑
3. � Mock 外部依赖
4. ✅ 测试覆盖率目标

## 总结

1. ✅ 测试环境搭建
2. ✅ Model 测试
3. ✅ CRUD 测试
4. ✅ 测试最佳实践

---

- **上一篇**: [10 事务与并发](../10-transactions.mdx)
- **下一篇**: [12 生产部署](../12-production-deployment.mdx)
