# 博客全站访问密码 —— 所有内容（文章正文 + 列表）用此密码加密，
# 只有输入正确密码才能查看。密码本身不会出现在任何构建产物中。
#
# 本地开发：
#   cp .env.example .env.local
#   然后在 .env.local 中填入你的密码（.env.local 已被 .gitignore 忽略）
#
# 线上部署：
#   GitHub 仓库 → Settings → Secrets and variables → Actions → New repository secret
#   Name: BLOG_ACCESS_PASSWORD   Value: 你的密码
#
# 安全建议：使用强密码（≥12 位，字母 + 数字 + 符号混合）。
# 注意：改密码后需重新构建；旧产物用旧密码加密，换密码后需重新部署。
BLOG_ACCESS_PASSWORD=your-strong-password-here
